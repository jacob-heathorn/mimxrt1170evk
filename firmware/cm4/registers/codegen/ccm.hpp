#pragma once

#include <stddef.h>
#include <stdint.h>
#include <cstring>

// CCM
//
// NOTE: This file was generated from the forge CMSIS-svd wrapper tool.
namespace nCCM {


// Clock group control
union CLOCK_GROUP0_CONTROL {
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running
    eOFF_0 = 0,
    // Turn off clock
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider0
    uint32_t DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 7;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP0_CONTROL() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP0_CONTROL &ref() { return *reinterpret_cast<volatile CLOCK_GROUP0_CONTROL*>(0x40CC4000); }
};

// Clock group control
union CLOCK_GROUP0_CONTROL_SET {
  
  // Bit field definition.
  struct {
    // read-write - Clock divider0
    uint32_t DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    uint32_t OFF : 1;
    uint32_t _reserved_1 : 7;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP0_CONTROL_SET() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP0_CONTROL_SET &ref() { return *reinterpret_cast<volatile CLOCK_GROUP0_CONTROL_SET*>(0x40CC4004); }
};

// Clock group control
union CLOCK_GROUP0_CONTROL_CLR {
  
  // Bit field definition.
  struct {
    // read-write - Clock divider0
    uint32_t DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    uint32_t OFF : 1;
    uint32_t _reserved_1 : 7;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP0_CONTROL_CLR() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP0_CONTROL_CLR &ref() { return *reinterpret_cast<volatile CLOCK_GROUP0_CONTROL_CLR*>(0x40CC4008); }
};

// Clock group control
union CLOCK_GROUP0_CONTROL_TOG {
  
  // Bit field definition.
  struct {
    // read-write - Clock divider0
    uint32_t DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    uint32_t OFF : 1;
    uint32_t _reserved_1 : 7;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP0_CONTROL_TOG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP0_CONTROL_TOG &ref() { return *reinterpret_cast<volatile CLOCK_GROUP0_CONTROL_TOG*>(0x40CC400C); }
};

// Clock group working status
union CLOCK_GROUP0_STATUS0 {
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Current clock root POWERDOWN setting
  enum class ePOWERDOWN : uint32_t {
    // Clock root is running
    eRUN = 0,
    // Clock root is Powered Down
    ePWRDWN = 1,
  };
  
  // Internal updating in generation logic
  enum class eSLICE_BUSY : uint32_t {
    // Clock generation logic is not busy
    eDIS = 0,
    // Clock generation logic is applying the new setting
    eENABLE = 1,
  };
  
  // Internal status synchronization to clock generation logic
  enum class eUPDATE_FORWARD : uint32_t {
    // Synchronization not in process
    eDIS = 0,
    // Synchronization in process
    eENABLE = 1,
  };
  
  // Internal status synchronization from clock generation logic
  enum class eUPDATE_REVERSE : uint32_t {
    // Synchronization not in process
    eDIS = 0,
    // Synchronization in process
    eENABLE = 1,
  };
  
  // Internal updating in clock group
  enum class eCHANGING : uint32_t {
    // Clock root is not updating currently
    eDIS = 0,
    // Clock root logic is updating currently
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    uint32_t DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock divider
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 2;
    // read-only - Current clock root POWERDOWN setting
    ePOWERDOWN POWERDOWN : 1;
    // read-only - Internal updating in generation logic
    eSLICE_BUSY SLICE_BUSY : 1;
    // read-only - Internal status synchronization to clock generation logic
    eUPDATE_FORWARD UPDATE_FORWARD : 1;
    // read-only - Internal status synchronization from clock generation logic
    eUPDATE_REVERSE UPDATE_REVERSE : 1;
    // read-only - Internal updating in clock group
    eCHANGING CHANGING : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP0_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP0_STATUS0 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP0_STATUS0*>(0x40CC4020); }
};

// Clock group low power/extend status
union CLOCK_GROUP0_STATUS1 {
  
  // Clock frequency decrease request
  enum class eDOWN_REQUEST : uint32_t {
    // No handshake signal is not requested
    eNOTREQ = 0,
    // Handshake signal with GPC status indicating frequency decrease is requested
    eREQUEST = 1,
  };
  
  // Clock frequency decrease complete
  enum class eDOWN_DONE : uint32_t {
    // Handshake signal with GPC status indicating frequency decrease is not complete
    eDISABLE = 0,
    // Handshake signal with GPC status indicating frequency decrease is complete
    eCOMPL = 1,
  };
  
  // Clock frequency increase request
  enum class eUP_REQUEST : uint32_t {
    // No handshake signal is not requested
    eNOTREQ = 0,
    // Handshake signal with GPC status indicating frequency increase is requested
    eREQUEST = 1,
  };
  
  // Clock frequency increase complete
  enum class eUP_DONE : uint32_t {
    // Handshake signal with GPC status indicating frequency increase is not complete
    eDISABLE = 0,
    // Handshake signal with GPC status indicating frequency increase is complete
    eCOMPL = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 16;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock frequency decrease request
    eDOWN_REQUEST DOWN_REQUEST : 1;
    // read-only - Clock frequency decrease complete
    eDOWN_DONE DOWN_DONE : 1;
    // read-only - Clock frequency increase request
    eUP_REQUEST UP_REQUEST : 1;
    // read-only - Clock frequency increase complete
    eUP_DONE UP_DONE : 1;
    uint32_t _reserved_1 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP0_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP0_STATUS1 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP0_STATUS1*>(0x40CC4024); }
};

// Clock group configuration
union CLOCK_GROUP0_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP0_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP0_CONFIG &ref() { return *reinterpret_cast<volatile CLOCK_GROUP0_CONFIG*>(0x40CC402C); }
};

// Clock group access control
union CLOCK_GROUP0_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // Clock cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // Clock can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // Lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode.
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode.
    eDOMAIN_MODE_1 = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    uint32_t SETPOINT_MODE : 1;
    uint32_t _reserved_3 : 2;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP0_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP0_AUTHEN &ref() { return *reinterpret_cast<volatile CLOCK_GROUP0_AUTHEN*>(0x40CC4030); }
};

// Clock group access control
union CLOCK_GROUP0_AUTHEN_SET {
  
  // Bit field definition.
  struct {
    // read-write - User access
    uint32_t TZ_USER : 1;
    // read-write - Non-secure access
    uint32_t TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    uint32_t LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    uint32_t LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    uint32_t DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    uint32_t SETPOINT_MODE : 1;
    uint32_t _reserved_3 : 2;
    // read-write - Lock low power and access mode
    uint32_t LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP0_AUTHEN_SET() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP0_AUTHEN_SET &ref() { return *reinterpret_cast<volatile CLOCK_GROUP0_AUTHEN_SET*>(0x40CC4034); }
};

// Clock group access control
union CLOCK_GROUP0_AUTHEN_CLR {
  
  // Bit field definition.
  struct {
    // read-write - User access
    uint32_t TZ_USER : 1;
    // read-write - Non-secure access
    uint32_t TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    uint32_t LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    uint32_t LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    uint32_t DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    uint32_t SETPOINT_MODE : 1;
    uint32_t _reserved_3 : 2;
    // read-write - Lock low power and access mode
    uint32_t LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP0_AUTHEN_CLR() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP0_AUTHEN_CLR &ref() { return *reinterpret_cast<volatile CLOCK_GROUP0_AUTHEN_CLR*>(0x40CC4038); }
};

// Clock group access control
union CLOCK_GROUP0_AUTHEN_TOG {
  
  // Bit field definition.
  struct {
    // read-write - User access
    uint32_t TZ_USER : 1;
    // read-write - Non-secure access
    uint32_t TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    uint32_t LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    uint32_t LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    uint32_t DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    uint32_t SETPOINT_MODE : 1;
    uint32_t _reserved_3 : 2;
    // read-write - Lock low power and access mode
    uint32_t LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP0_AUTHEN_TOG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP0_AUTHEN_TOG &ref() { return *reinterpret_cast<volatile CLOCK_GROUP0_AUTHEN_TOG*>(0x40CC403C); }
};

// Setpoint setting
union CLOCK_GROUP0_SETPOINT0 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP0_SETPOINT0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP0_SETPOINT0 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP0_SETPOINT0*>(0x40CC4040); }
};

// Setpoint setting
union CLOCK_GROUP0_SETPOINT1 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP0_SETPOINT1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP0_SETPOINT1 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP0_SETPOINT1*>(0x40CC4044); }
};

// Setpoint setting
union CLOCK_GROUP0_SETPOINT2 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP0_SETPOINT2() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP0_SETPOINT2 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP0_SETPOINT2*>(0x40CC4048); }
};

// Setpoint setting
union CLOCK_GROUP0_SETPOINT3 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP0_SETPOINT3() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP0_SETPOINT3 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP0_SETPOINT3*>(0x40CC404C); }
};

// Setpoint setting
union CLOCK_GROUP0_SETPOINT4 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP0_SETPOINT4() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP0_SETPOINT4 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP0_SETPOINT4*>(0x40CC4050); }
};

// Setpoint setting
union CLOCK_GROUP0_SETPOINT5 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP0_SETPOINT5() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP0_SETPOINT5 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP0_SETPOINT5*>(0x40CC4054); }
};

// Setpoint setting
union CLOCK_GROUP0_SETPOINT6 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP0_SETPOINT6() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP0_SETPOINT6 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP0_SETPOINT6*>(0x40CC4058); }
};

// Setpoint setting
union CLOCK_GROUP0_SETPOINT7 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP0_SETPOINT7() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP0_SETPOINT7 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP0_SETPOINT7*>(0x40CC405C); }
};

// Setpoint setting
union CLOCK_GROUP0_SETPOINT8 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP0_SETPOINT8() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP0_SETPOINT8 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP0_SETPOINT8*>(0x40CC4060); }
};

// Setpoint setting
union CLOCK_GROUP0_SETPOINT9 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP0_SETPOINT9() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP0_SETPOINT9 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP0_SETPOINT9*>(0x40CC4064); }
};

// Setpoint setting
union CLOCK_GROUP0_SETPOINT10 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP0_SETPOINT10() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP0_SETPOINT10 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP0_SETPOINT10*>(0x40CC4068); }
};

// Setpoint setting
union CLOCK_GROUP0_SETPOINT11 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP0_SETPOINT11() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP0_SETPOINT11 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP0_SETPOINT11*>(0x40CC406C); }
};

// Setpoint setting
union CLOCK_GROUP0_SETPOINT12 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP0_SETPOINT12() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP0_SETPOINT12 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP0_SETPOINT12*>(0x40CC4070); }
};

// Setpoint setting
union CLOCK_GROUP0_SETPOINT13 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP0_SETPOINT13() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP0_SETPOINT13 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP0_SETPOINT13*>(0x40CC4074); }
};

// Setpoint setting
union CLOCK_GROUP0_SETPOINT14 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP0_SETPOINT14() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP0_SETPOINT14 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP0_SETPOINT14*>(0x40CC4078); }
};

// Setpoint setting
union CLOCK_GROUP0_SETPOINT15 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP0_SETPOINT15() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP0_SETPOINT15 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP0_SETPOINT15*>(0x40CC407C); }
};

// Clock group control
union CLOCK_GROUP1_CONTROL {
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running
    eOFF_0 = 0,
    // Turn off clock
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider0
    uint32_t DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 7;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP1_CONTROL() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP1_CONTROL &ref() { return *reinterpret_cast<volatile CLOCK_GROUP1_CONTROL*>(0x40CC4080); }
};

// Clock group control
union CLOCK_GROUP1_CONTROL_SET {
  
  // Bit field definition.
  struct {
    // read-write - Clock divider0
    uint32_t DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    uint32_t OFF : 1;
    uint32_t _reserved_1 : 7;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP1_CONTROL_SET() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP1_CONTROL_SET &ref() { return *reinterpret_cast<volatile CLOCK_GROUP1_CONTROL_SET*>(0x40CC4084); }
};

// Clock group control
union CLOCK_GROUP1_CONTROL_CLR {
  
  // Bit field definition.
  struct {
    // read-write - Clock divider0
    uint32_t DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    uint32_t OFF : 1;
    uint32_t _reserved_1 : 7;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP1_CONTROL_CLR() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP1_CONTROL_CLR &ref() { return *reinterpret_cast<volatile CLOCK_GROUP1_CONTROL_CLR*>(0x40CC4088); }
};

// Clock group control
union CLOCK_GROUP1_CONTROL_TOG {
  
  // Bit field definition.
  struct {
    // read-write - Clock divider0
    uint32_t DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    uint32_t OFF : 1;
    uint32_t _reserved_1 : 7;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP1_CONTROL_TOG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP1_CONTROL_TOG &ref() { return *reinterpret_cast<volatile CLOCK_GROUP1_CONTROL_TOG*>(0x40CC408C); }
};

// Clock group working status
union CLOCK_GROUP1_STATUS0 {
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Current clock root POWERDOWN setting
  enum class ePOWERDOWN : uint32_t {
    // Clock root is running
    eRUN = 0,
    // Clock root is Powered Down
    ePWRDWN = 1,
  };
  
  // Internal updating in generation logic
  enum class eSLICE_BUSY : uint32_t {
    // Clock generation logic is not busy
    eDIS = 0,
    // Clock generation logic is applying the new setting
    eENABLE = 1,
  };
  
  // Internal status synchronization to clock generation logic
  enum class eUPDATE_FORWARD : uint32_t {
    // Synchronization not in process
    eDIS = 0,
    // Synchronization in process
    eENABLE = 1,
  };
  
  // Internal status synchronization from clock generation logic
  enum class eUPDATE_REVERSE : uint32_t {
    // Synchronization not in process
    eDIS = 0,
    // Synchronization in process
    eENABLE = 1,
  };
  
  // Internal updating in clock group
  enum class eCHANGING : uint32_t {
    // Clock root is not updating currently
    eDIS = 0,
    // Clock root logic is updating currently
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    uint32_t DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock divider
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 2;
    // read-only - Current clock root POWERDOWN setting
    ePOWERDOWN POWERDOWN : 1;
    // read-only - Internal updating in generation logic
    eSLICE_BUSY SLICE_BUSY : 1;
    // read-only - Internal status synchronization to clock generation logic
    eUPDATE_FORWARD UPDATE_FORWARD : 1;
    // read-only - Internal status synchronization from clock generation logic
    eUPDATE_REVERSE UPDATE_REVERSE : 1;
    // read-only - Internal updating in clock group
    eCHANGING CHANGING : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP1_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP1_STATUS0 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP1_STATUS0*>(0x40CC40A0); }
};

// Clock group low power/extend status
union CLOCK_GROUP1_STATUS1 {
  
  // Clock frequency decrease request
  enum class eDOWN_REQUEST : uint32_t {
    // No handshake signal is not requested
    eNOTREQ = 0,
    // Handshake signal with GPC status indicating frequency decrease is requested
    eREQUEST = 1,
  };
  
  // Clock frequency decrease complete
  enum class eDOWN_DONE : uint32_t {
    // Handshake signal with GPC status indicating frequency decrease is not complete
    eDISABLE = 0,
    // Handshake signal with GPC status indicating frequency decrease is complete
    eCOMPL = 1,
  };
  
  // Clock frequency increase request
  enum class eUP_REQUEST : uint32_t {
    // No handshake signal is not requested
    eNOTREQ = 0,
    // Handshake signal with GPC status indicating frequency increase is requested
    eREQUEST = 1,
  };
  
  // Clock frequency increase complete
  enum class eUP_DONE : uint32_t {
    // Handshake signal with GPC status indicating frequency increase is not complete
    eDISABLE = 0,
    // Handshake signal with GPC status indicating frequency increase is complete
    eCOMPL = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 16;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock frequency decrease request
    eDOWN_REQUEST DOWN_REQUEST : 1;
    // read-only - Clock frequency decrease complete
    eDOWN_DONE DOWN_DONE : 1;
    // read-only - Clock frequency increase request
    eUP_REQUEST UP_REQUEST : 1;
    // read-only - Clock frequency increase complete
    eUP_DONE UP_DONE : 1;
    uint32_t _reserved_1 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP1_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP1_STATUS1 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP1_STATUS1*>(0x40CC40A4); }
};

// Clock group configuration
union CLOCK_GROUP1_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP1_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP1_CONFIG &ref() { return *reinterpret_cast<volatile CLOCK_GROUP1_CONFIG*>(0x40CC40AC); }
};

// Clock group access control
union CLOCK_GROUP1_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // Clock cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // Clock can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // Lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode.
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode.
    eDOMAIN_MODE_1 = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    uint32_t SETPOINT_MODE : 1;
    uint32_t _reserved_3 : 2;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP1_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP1_AUTHEN &ref() { return *reinterpret_cast<volatile CLOCK_GROUP1_AUTHEN*>(0x40CC40B0); }
};

// Clock group access control
union CLOCK_GROUP1_AUTHEN_SET {
  
  // Bit field definition.
  struct {
    // read-write - User access
    uint32_t TZ_USER : 1;
    // read-write - Non-secure access
    uint32_t TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    uint32_t LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    uint32_t LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    uint32_t DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    uint32_t SETPOINT_MODE : 1;
    uint32_t _reserved_3 : 2;
    // read-write - Lock low power and access mode
    uint32_t LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP1_AUTHEN_SET() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP1_AUTHEN_SET &ref() { return *reinterpret_cast<volatile CLOCK_GROUP1_AUTHEN_SET*>(0x40CC40B4); }
};

// Clock group access control
union CLOCK_GROUP1_AUTHEN_CLR {
  
  // Bit field definition.
  struct {
    // read-write - User access
    uint32_t TZ_USER : 1;
    // read-write - Non-secure access
    uint32_t TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    uint32_t LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    uint32_t LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    uint32_t DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    uint32_t SETPOINT_MODE : 1;
    uint32_t _reserved_3 : 2;
    // read-write - Lock low power and access mode
    uint32_t LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP1_AUTHEN_CLR() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP1_AUTHEN_CLR &ref() { return *reinterpret_cast<volatile CLOCK_GROUP1_AUTHEN_CLR*>(0x40CC40B8); }
};

// Clock group access control
union CLOCK_GROUP1_AUTHEN_TOG {
  
  // Bit field definition.
  struct {
    // read-write - User access
    uint32_t TZ_USER : 1;
    // read-write - Non-secure access
    uint32_t TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    uint32_t LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    uint32_t LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    uint32_t DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    uint32_t SETPOINT_MODE : 1;
    uint32_t _reserved_3 : 2;
    // read-write - Lock low power and access mode
    uint32_t LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP1_AUTHEN_TOG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP1_AUTHEN_TOG &ref() { return *reinterpret_cast<volatile CLOCK_GROUP1_AUTHEN_TOG*>(0x40CC40BC); }
};

// Setpoint setting
union CLOCK_GROUP1_SETPOINT0 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP1_SETPOINT0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP1_SETPOINT0 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP1_SETPOINT0*>(0x40CC40C0); }
};

// Setpoint setting
union CLOCK_GROUP1_SETPOINT1 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP1_SETPOINT1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP1_SETPOINT1 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP1_SETPOINT1*>(0x40CC40C4); }
};

// Setpoint setting
union CLOCK_GROUP1_SETPOINT2 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP1_SETPOINT2() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP1_SETPOINT2 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP1_SETPOINT2*>(0x40CC40C8); }
};

// Setpoint setting
union CLOCK_GROUP1_SETPOINT3 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP1_SETPOINT3() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP1_SETPOINT3 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP1_SETPOINT3*>(0x40CC40CC); }
};

// Setpoint setting
union CLOCK_GROUP1_SETPOINT4 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP1_SETPOINT4() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP1_SETPOINT4 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP1_SETPOINT4*>(0x40CC40D0); }
};

// Setpoint setting
union CLOCK_GROUP1_SETPOINT5 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP1_SETPOINT5() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP1_SETPOINT5 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP1_SETPOINT5*>(0x40CC40D4); }
};

// Setpoint setting
union CLOCK_GROUP1_SETPOINT6 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP1_SETPOINT6() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP1_SETPOINT6 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP1_SETPOINT6*>(0x40CC40D8); }
};

// Setpoint setting
union CLOCK_GROUP1_SETPOINT7 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP1_SETPOINT7() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP1_SETPOINT7 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP1_SETPOINT7*>(0x40CC40DC); }
};

// Setpoint setting
union CLOCK_GROUP1_SETPOINT8 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP1_SETPOINT8() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP1_SETPOINT8 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP1_SETPOINT8*>(0x40CC40E0); }
};

// Setpoint setting
union CLOCK_GROUP1_SETPOINT9 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP1_SETPOINT9() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP1_SETPOINT9 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP1_SETPOINT9*>(0x40CC40E4); }
};

// Setpoint setting
union CLOCK_GROUP1_SETPOINT10 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP1_SETPOINT10() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP1_SETPOINT10 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP1_SETPOINT10*>(0x40CC40E8); }
};

// Setpoint setting
union CLOCK_GROUP1_SETPOINT11 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP1_SETPOINT11() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP1_SETPOINT11 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP1_SETPOINT11*>(0x40CC40EC); }
};

// Setpoint setting
union CLOCK_GROUP1_SETPOINT12 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP1_SETPOINT12() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP1_SETPOINT12 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP1_SETPOINT12*>(0x40CC40F0); }
};

// Setpoint setting
union CLOCK_GROUP1_SETPOINT13 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP1_SETPOINT13() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP1_SETPOINT13 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP1_SETPOINT13*>(0x40CC40F4); }
};

// Setpoint setting
union CLOCK_GROUP1_SETPOINT14 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP1_SETPOINT14() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP1_SETPOINT14 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP1_SETPOINT14*>(0x40CC40F8); }
};

// Setpoint setting
union CLOCK_GROUP1_SETPOINT15 {
  
  // Clock divider
  enum class eDIV0 : uint32_t {
    // Direct output.
    eDIV0_0 = 0,
    // Divide by 2.
    eDIV0_1 = 1,
    // Divide by 3.
    eDIV0_2 = 2,
    // Divide by 4.
    eDIV0_3 = 3,
    // Divide by 16.
    eDIV0_15 = 15,
  };
  
  // OFF
  enum class eOFF : uint32_t {
    // Clock is running.
    eOFF_0 = 0,
    // Turn off clock.
    eOFF_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - Clock divider
    eDIV0 DIV0 : 4;
    uint32_t _reserved_0 : 12;
    // read-write - Clock group global restart count
    uint32_t RSTDIV : 8;
    // read-write - OFF
    eOFF OFF : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Grade
    uint32_t GRADE : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  CLOCK_GROUP1_SETPOINT15() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile CLOCK_GROUP1_SETPOINT15 &ref() { return *reinterpret_cast<volatile CLOCK_GROUP1_SETPOINT15*>(0x40CC40FC); }
};

// General Purpose Register
union GPR_PRIVATE1 {
  
  // Bit field definition.
  struct {
    // read-write - GP register
    uint32_t GPR : 32;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE1 &ref() { return *reinterpret_cast<volatile GPR_PRIVATE1*>(0x40CC4C20); }
};

// General Purpose Register
union GPR_PRIVATE1_SET {
  
  // Bit field definition.
  struct {
    // read-write - GP register
    uint32_t GPR : 32;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE1_SET() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE1_SET &ref() { return *reinterpret_cast<volatile GPR_PRIVATE1_SET*>(0x40CC4C24); }
};

// General Purpose Register
union GPR_PRIVATE1_CLR {
  
  // Bit field definition.
  struct {
    // read-write - GP register
    uint32_t GPR : 32;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE1_CLR() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE1_CLR &ref() { return *reinterpret_cast<volatile GPR_PRIVATE1_CLR*>(0x40CC4C28); }
};

// General Purpose Register
union GPR_PRIVATE1_TOG {
  
  // Bit field definition.
  struct {
    // read-write - GP register
    uint32_t GPR : 32;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE1_TOG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE1_TOG &ref() { return *reinterpret_cast<volatile GPR_PRIVATE1_TOG*>(0x40CC4C2C); }
};

// GPR access control
union GPR_PRIVATE1_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // Clock cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // Clock can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // Lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Whitelist
  enum class eWHITE_LIST : uint32_t {
    // This domain is NOT allowed to change clock.
    eWHITE_LIST_0 = 0,
    // This domain is allowed to change clock.
    eWHITE_LIST_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by Domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does NOT work in Domain Mode.
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode.
    eDOMAIN_MODE_1 = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    eWHITE_LIST WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by Domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    uint32_t _reserved_3 : 3;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE1_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE1_AUTHEN &ref() { return *reinterpret_cast<volatile GPR_PRIVATE1_AUTHEN*>(0x40CC4C30); }
};

// GPR access control
union GPR_PRIVATE1_AUTHEN_SET {
  
  // Bit field definition.
  struct {
    // read-write - User access
    uint32_t TZ_USER : 1;
    // read-write - Non-secure access
    uint32_t TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    uint32_t LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    uint32_t LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by Domain
    uint32_t DOMAIN_MODE : 1;
    uint32_t _reserved_3 : 3;
    // read-write - Lock low power and access mode
    uint32_t LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE1_AUTHEN_SET() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE1_AUTHEN_SET &ref() { return *reinterpret_cast<volatile GPR_PRIVATE1_AUTHEN_SET*>(0x40CC4C34); }
};

// GPR access control
union GPR_PRIVATE1_AUTHEN_CLR {
  
  // Bit field definition.
  struct {
    // read-write - User access
    uint32_t TZ_USER : 1;
    // read-write - Non-secure access
    uint32_t TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    uint32_t LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    uint32_t LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by Domain
    uint32_t DOMAIN_MODE : 1;
    uint32_t _reserved_3 : 3;
    // read-write - Lock low power and access mode
    uint32_t LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE1_AUTHEN_CLR() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE1_AUTHEN_CLR &ref() { return *reinterpret_cast<volatile GPR_PRIVATE1_AUTHEN_CLR*>(0x40CC4C38); }
};

// GPR access control
union GPR_PRIVATE1_AUTHEN_TOG {
  
  // Bit field definition.
  struct {
    // read-write - User access
    uint32_t TZ_USER : 1;
    // read-write - Non-secure access
    uint32_t TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    uint32_t LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    uint32_t LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by Domain
    uint32_t DOMAIN_MODE : 1;
    uint32_t _reserved_3 : 3;
    // read-write - Lock low power and access mode
    uint32_t LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE1_AUTHEN_TOG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE1_AUTHEN_TOG &ref() { return *reinterpret_cast<volatile GPR_PRIVATE1_AUTHEN_TOG*>(0x40CC4C3C); }
};

// General Purpose Register
union GPR_PRIVATE2 {
  
  // Bit field definition.
  struct {
    // read-write - GP register
    uint32_t GPR : 32;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE2() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE2 &ref() { return *reinterpret_cast<volatile GPR_PRIVATE2*>(0x40CC4C40); }
};

// General Purpose Register
union GPR_PRIVATE2_SET {
  
  // Bit field definition.
  struct {
    // read-write - GP register
    uint32_t GPR : 32;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE2_SET() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE2_SET &ref() { return *reinterpret_cast<volatile GPR_PRIVATE2_SET*>(0x40CC4C44); }
};

// General Purpose Register
union GPR_PRIVATE2_CLR {
  
  // Bit field definition.
  struct {
    // read-write - GP register
    uint32_t GPR : 32;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE2_CLR() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE2_CLR &ref() { return *reinterpret_cast<volatile GPR_PRIVATE2_CLR*>(0x40CC4C48); }
};

// General Purpose Register
union GPR_PRIVATE2_TOG {
  
  // Bit field definition.
  struct {
    // read-write - GP register
    uint32_t GPR : 32;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE2_TOG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE2_TOG &ref() { return *reinterpret_cast<volatile GPR_PRIVATE2_TOG*>(0x40CC4C4C); }
};

// GPR access control
union GPR_PRIVATE2_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // Clock cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // Clock can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // Lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Whitelist
  enum class eWHITE_LIST : uint32_t {
    // This domain is NOT allowed to change clock.
    eWHITE_LIST_0 = 0,
    // This domain is allowed to change clock.
    eWHITE_LIST_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by Domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does NOT work in Domain Mode.
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode.
    eDOMAIN_MODE_1 = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    eWHITE_LIST WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by Domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    uint32_t _reserved_3 : 3;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE2_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE2_AUTHEN &ref() { return *reinterpret_cast<volatile GPR_PRIVATE2_AUTHEN*>(0x40CC4C50); }
};

// GPR access control
union GPR_PRIVATE2_AUTHEN_SET {
  
  // Bit field definition.
  struct {
    // read-write - User access
    uint32_t TZ_USER : 1;
    // read-write - Non-secure access
    uint32_t TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    uint32_t LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    uint32_t LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by Domain
    uint32_t DOMAIN_MODE : 1;
    uint32_t _reserved_3 : 3;
    // read-write - Lock low power and access mode
    uint32_t LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE2_AUTHEN_SET() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE2_AUTHEN_SET &ref() { return *reinterpret_cast<volatile GPR_PRIVATE2_AUTHEN_SET*>(0x40CC4C54); }
};

// GPR access control
union GPR_PRIVATE2_AUTHEN_CLR {
  
  // Bit field definition.
  struct {
    // read-write - User access
    uint32_t TZ_USER : 1;
    // read-write - Non-secure access
    uint32_t TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    uint32_t LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    uint32_t LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by Domain
    uint32_t DOMAIN_MODE : 1;
    uint32_t _reserved_3 : 3;
    // read-write - Lock low power and access mode
    uint32_t LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE2_AUTHEN_CLR() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE2_AUTHEN_CLR &ref() { return *reinterpret_cast<volatile GPR_PRIVATE2_AUTHEN_CLR*>(0x40CC4C58); }
};

// GPR access control
union GPR_PRIVATE2_AUTHEN_TOG {
  
  // Bit field definition.
  struct {
    // read-write - User access
    uint32_t TZ_USER : 1;
    // read-write - Non-secure access
    uint32_t TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    uint32_t LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    uint32_t LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by Domain
    uint32_t DOMAIN_MODE : 1;
    uint32_t _reserved_3 : 3;
    // read-write - Lock low power and access mode
    uint32_t LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE2_AUTHEN_TOG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE2_AUTHEN_TOG &ref() { return *reinterpret_cast<volatile GPR_PRIVATE2_AUTHEN_TOG*>(0x40CC4C5C); }
};

// General Purpose Register
union GPR_PRIVATE3 {
  
  // Bit field definition.
  struct {
    // read-write - GP register
    uint32_t GPR : 32;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE3() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE3 &ref() { return *reinterpret_cast<volatile GPR_PRIVATE3*>(0x40CC4C60); }
};

// General Purpose Register
union GPR_PRIVATE3_SET {
  
  // Bit field definition.
  struct {
    // read-write - GP register
    uint32_t GPR : 32;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE3_SET() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE3_SET &ref() { return *reinterpret_cast<volatile GPR_PRIVATE3_SET*>(0x40CC4C64); }
};

// General Purpose Register
union GPR_PRIVATE3_CLR {
  
  // Bit field definition.
  struct {
    // read-write - GP register
    uint32_t GPR : 32;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE3_CLR() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE3_CLR &ref() { return *reinterpret_cast<volatile GPR_PRIVATE3_CLR*>(0x40CC4C68); }
};

// General Purpose Register
union GPR_PRIVATE3_TOG {
  
  // Bit field definition.
  struct {
    // read-write - GP register
    uint32_t GPR : 32;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE3_TOG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE3_TOG &ref() { return *reinterpret_cast<volatile GPR_PRIVATE3_TOG*>(0x40CC4C6C); }
};

// GPR access control
union GPR_PRIVATE3_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // Clock cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // Clock can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // Lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Whitelist
  enum class eWHITE_LIST : uint32_t {
    // This domain is NOT allowed to change clock.
    eWHITE_LIST_0 = 0,
    // This domain is allowed to change clock.
    eWHITE_LIST_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by Domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does NOT work in Domain Mode.
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode.
    eDOMAIN_MODE_1 = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    eWHITE_LIST WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by Domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    uint32_t _reserved_3 : 3;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE3_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE3_AUTHEN &ref() { return *reinterpret_cast<volatile GPR_PRIVATE3_AUTHEN*>(0x40CC4C70); }
};

// GPR access control
union GPR_PRIVATE3_AUTHEN_SET {
  
  // Bit field definition.
  struct {
    // read-write - User access
    uint32_t TZ_USER : 1;
    // read-write - Non-secure access
    uint32_t TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    uint32_t LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    uint32_t LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by Domain
    uint32_t DOMAIN_MODE : 1;
    uint32_t _reserved_3 : 3;
    // read-write - Lock low power and access mode
    uint32_t LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE3_AUTHEN_SET() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE3_AUTHEN_SET &ref() { return *reinterpret_cast<volatile GPR_PRIVATE3_AUTHEN_SET*>(0x40CC4C74); }
};

// GPR access control
union GPR_PRIVATE3_AUTHEN_CLR {
  
  // Bit field definition.
  struct {
    // read-write - User access
    uint32_t TZ_USER : 1;
    // read-write - Non-secure access
    uint32_t TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    uint32_t LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    uint32_t LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by Domain
    uint32_t DOMAIN_MODE : 1;
    uint32_t _reserved_3 : 3;
    // read-write - Lock low power and access mode
    uint32_t LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE3_AUTHEN_CLR() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE3_AUTHEN_CLR &ref() { return *reinterpret_cast<volatile GPR_PRIVATE3_AUTHEN_CLR*>(0x40CC4C78); }
};

// GPR access control
union GPR_PRIVATE3_AUTHEN_TOG {
  
  // Bit field definition.
  struct {
    // read-write - User access
    uint32_t TZ_USER : 1;
    // read-write - Non-secure access
    uint32_t TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    uint32_t LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    uint32_t LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by Domain
    uint32_t DOMAIN_MODE : 1;
    uint32_t _reserved_3 : 3;
    // read-write - Lock low power and access mode
    uint32_t LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE3_AUTHEN_TOG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE3_AUTHEN_TOG &ref() { return *reinterpret_cast<volatile GPR_PRIVATE3_AUTHEN_TOG*>(0x40CC4C7C); }
};

// General Purpose Register
union GPR_PRIVATE4 {
  
  // Bit field definition.
  struct {
    // read-write - GP register
    uint32_t GPR : 32;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE4() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE4 &ref() { return *reinterpret_cast<volatile GPR_PRIVATE4*>(0x40CC4C80); }
};

// General Purpose Register
union GPR_PRIVATE4_SET {
  
  // Bit field definition.
  struct {
    // read-write - GP register
    uint32_t GPR : 32;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE4_SET() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE4_SET &ref() { return *reinterpret_cast<volatile GPR_PRIVATE4_SET*>(0x40CC4C84); }
};

// General Purpose Register
union GPR_PRIVATE4_CLR {
  
  // Bit field definition.
  struct {
    // read-write - GP register
    uint32_t GPR : 32;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE4_CLR() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE4_CLR &ref() { return *reinterpret_cast<volatile GPR_PRIVATE4_CLR*>(0x40CC4C88); }
};

// General Purpose Register
union GPR_PRIVATE4_TOG {
  
  // Bit field definition.
  struct {
    // read-write - GP register
    uint32_t GPR : 32;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE4_TOG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE4_TOG &ref() { return *reinterpret_cast<volatile GPR_PRIVATE4_TOG*>(0x40CC4C8C); }
};

// GPR access control
union GPR_PRIVATE4_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // Clock cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // Clock can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // Lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Whitelist
  enum class eWHITE_LIST : uint32_t {
    // This domain is NOT allowed to change clock.
    eWHITE_LIST_0 = 0,
    // This domain is allowed to change clock.
    eWHITE_LIST_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by Domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does NOT work in Domain Mode.
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode.
    eDOMAIN_MODE_1 = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    eWHITE_LIST WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by Domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    uint32_t _reserved_3 : 3;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE4_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE4_AUTHEN &ref() { return *reinterpret_cast<volatile GPR_PRIVATE4_AUTHEN*>(0x40CC4C90); }
};

// GPR access control
union GPR_PRIVATE4_AUTHEN_SET {
  
  // Bit field definition.
  struct {
    // read-write - User access
    uint32_t TZ_USER : 1;
    // read-write - Non-secure access
    uint32_t TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    uint32_t LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    uint32_t LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by Domain
    uint32_t DOMAIN_MODE : 1;
    uint32_t _reserved_3 : 3;
    // read-write - Lock low power and access mode
    uint32_t LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE4_AUTHEN_SET() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE4_AUTHEN_SET &ref() { return *reinterpret_cast<volatile GPR_PRIVATE4_AUTHEN_SET*>(0x40CC4C94); }
};

// GPR access control
union GPR_PRIVATE4_AUTHEN_CLR {
  
  // Bit field definition.
  struct {
    // read-write - User access
    uint32_t TZ_USER : 1;
    // read-write - Non-secure access
    uint32_t TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    uint32_t LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    uint32_t LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by Domain
    uint32_t DOMAIN_MODE : 1;
    uint32_t _reserved_3 : 3;
    // read-write - Lock low power and access mode
    uint32_t LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE4_AUTHEN_CLR() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE4_AUTHEN_CLR &ref() { return *reinterpret_cast<volatile GPR_PRIVATE4_AUTHEN_CLR*>(0x40CC4C98); }
};

// GPR access control
union GPR_PRIVATE4_AUTHEN_TOG {
  
  // Bit field definition.
  struct {
    // read-write - User access
    uint32_t TZ_USER : 1;
    // read-write - Non-secure access
    uint32_t TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    uint32_t LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    uint32_t LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by Domain
    uint32_t DOMAIN_MODE : 1;
    uint32_t _reserved_3 : 3;
    // read-write - Lock low power and access mode
    uint32_t LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE4_AUTHEN_TOG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE4_AUTHEN_TOG &ref() { return *reinterpret_cast<volatile GPR_PRIVATE4_AUTHEN_TOG*>(0x40CC4C9C); }
};

// General Purpose Register
union GPR_PRIVATE5 {
  
  // Bit field definition.
  struct {
    // read-write - GP register
    uint32_t GPR : 32;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE5() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE5 &ref() { return *reinterpret_cast<volatile GPR_PRIVATE5*>(0x40CC4CA0); }
};

// General Purpose Register
union GPR_PRIVATE5_SET {
  
  // Bit field definition.
  struct {
    // read-write - GP register
    uint32_t GPR : 32;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE5_SET() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE5_SET &ref() { return *reinterpret_cast<volatile GPR_PRIVATE5_SET*>(0x40CC4CA4); }
};

// General Purpose Register
union GPR_PRIVATE5_CLR {
  
  // Bit field definition.
  struct {
    // read-write - GP register
    uint32_t GPR : 32;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE5_CLR() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE5_CLR &ref() { return *reinterpret_cast<volatile GPR_PRIVATE5_CLR*>(0x40CC4CA8); }
};

// General Purpose Register
union GPR_PRIVATE5_TOG {
  
  // Bit field definition.
  struct {
    // read-write - GP register
    uint32_t GPR : 32;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE5_TOG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE5_TOG &ref() { return *reinterpret_cast<volatile GPR_PRIVATE5_TOG*>(0x40CC4CAC); }
};

// GPR access control
union GPR_PRIVATE5_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // Clock cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // Clock can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // Lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Whitelist
  enum class eWHITE_LIST : uint32_t {
    // This domain is NOT allowed to change clock.
    eWHITE_LIST_0 = 0,
    // This domain is allowed to change clock.
    eWHITE_LIST_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by Domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does NOT work in Domain Mode.
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode.
    eDOMAIN_MODE_1 = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    eWHITE_LIST WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by Domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    uint32_t _reserved_3 : 3;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE5_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE5_AUTHEN &ref() { return *reinterpret_cast<volatile GPR_PRIVATE5_AUTHEN*>(0x40CC4CB0); }
};

// GPR access control
union GPR_PRIVATE5_AUTHEN_SET {
  
  // Bit field definition.
  struct {
    // read-write - User access
    uint32_t TZ_USER : 1;
    // read-write - Non-secure access
    uint32_t TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    uint32_t LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    uint32_t LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by Domain
    uint32_t DOMAIN_MODE : 1;
    uint32_t _reserved_3 : 3;
    // read-write - Lock low power and access mode
    uint32_t LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE5_AUTHEN_SET() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE5_AUTHEN_SET &ref() { return *reinterpret_cast<volatile GPR_PRIVATE5_AUTHEN_SET*>(0x40CC4CB4); }
};

// GPR access control
union GPR_PRIVATE5_AUTHEN_CLR {
  
  // Bit field definition.
  struct {
    // read-write - User access
    uint32_t TZ_USER : 1;
    // read-write - Non-secure access
    uint32_t TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    uint32_t LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    uint32_t LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by Domain
    uint32_t DOMAIN_MODE : 1;
    uint32_t _reserved_3 : 3;
    // read-write - Lock low power and access mode
    uint32_t LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE5_AUTHEN_CLR() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE5_AUTHEN_CLR &ref() { return *reinterpret_cast<volatile GPR_PRIVATE5_AUTHEN_CLR*>(0x40CC4CB8); }
};

// GPR access control
union GPR_PRIVATE5_AUTHEN_TOG {
  
  // Bit field definition.
  struct {
    // read-write - User access
    uint32_t TZ_USER : 1;
    // read-write - Non-secure access
    uint32_t TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    uint32_t LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    uint32_t LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by Domain
    uint32_t DOMAIN_MODE : 1;
    uint32_t _reserved_3 : 3;
    // read-write - Lock low power and access mode
    uint32_t LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE5_AUTHEN_TOG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE5_AUTHEN_TOG &ref() { return *reinterpret_cast<volatile GPR_PRIVATE5_AUTHEN_TOG*>(0x40CC4CBC); }
};

// General Purpose Register
union GPR_PRIVATE6 {
  
  // Bit field definition.
  struct {
    // read-write - GP register
    uint32_t GPR : 32;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE6() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE6 &ref() { return *reinterpret_cast<volatile GPR_PRIVATE6*>(0x40CC4CC0); }
};

// General Purpose Register
union GPR_PRIVATE6_SET {
  
  // Bit field definition.
  struct {
    // read-write - GP register
    uint32_t GPR : 32;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE6_SET() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE6_SET &ref() { return *reinterpret_cast<volatile GPR_PRIVATE6_SET*>(0x40CC4CC4); }
};

// General Purpose Register
union GPR_PRIVATE6_CLR {
  
  // Bit field definition.
  struct {
    // read-write - GP register
    uint32_t GPR : 32;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE6_CLR() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE6_CLR &ref() { return *reinterpret_cast<volatile GPR_PRIVATE6_CLR*>(0x40CC4CC8); }
};

// General Purpose Register
union GPR_PRIVATE6_TOG {
  
  // Bit field definition.
  struct {
    // read-write - GP register
    uint32_t GPR : 32;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE6_TOG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE6_TOG &ref() { return *reinterpret_cast<volatile GPR_PRIVATE6_TOG*>(0x40CC4CCC); }
};

// GPR access control
union GPR_PRIVATE6_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // Clock cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // Clock can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // Lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Whitelist
  enum class eWHITE_LIST : uint32_t {
    // This domain is NOT allowed to change clock.
    eWHITE_LIST_0 = 0,
    // This domain is allowed to change clock.
    eWHITE_LIST_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by Domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does NOT work in Domain Mode.
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode.
    eDOMAIN_MODE_1 = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    eWHITE_LIST WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by Domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    uint32_t _reserved_3 : 3;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE6_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE6_AUTHEN &ref() { return *reinterpret_cast<volatile GPR_PRIVATE6_AUTHEN*>(0x40CC4CD0); }
};

// GPR access control
union GPR_PRIVATE6_AUTHEN_SET {
  
  // Bit field definition.
  struct {
    // read-write - User access
    uint32_t TZ_USER : 1;
    // read-write - Non-secure access
    uint32_t TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    uint32_t LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    uint32_t LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by Domain
    uint32_t DOMAIN_MODE : 1;
    uint32_t _reserved_3 : 3;
    // read-write - Lock low power and access mode
    uint32_t LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE6_AUTHEN_SET() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE6_AUTHEN_SET &ref() { return *reinterpret_cast<volatile GPR_PRIVATE6_AUTHEN_SET*>(0x40CC4CD4); }
};

// GPR access control
union GPR_PRIVATE6_AUTHEN_CLR {
  
  // Bit field definition.
  struct {
    // read-write - User access
    uint32_t TZ_USER : 1;
    // read-write - Non-secure access
    uint32_t TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    uint32_t LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    uint32_t LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by Domain
    uint32_t DOMAIN_MODE : 1;
    uint32_t _reserved_3 : 3;
    // read-write - Lock low power and access mode
    uint32_t LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE6_AUTHEN_CLR() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE6_AUTHEN_CLR &ref() { return *reinterpret_cast<volatile GPR_PRIVATE6_AUTHEN_CLR*>(0x40CC4CD8); }
};

// GPR access control
union GPR_PRIVATE6_AUTHEN_TOG {
  
  // Bit field definition.
  struct {
    // read-write - User access
    uint32_t TZ_USER : 1;
    // read-write - Non-secure access
    uint32_t TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    uint32_t LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    uint32_t LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by Domain
    uint32_t DOMAIN_MODE : 1;
    uint32_t _reserved_3 : 3;
    // read-write - Lock low power and access mode
    uint32_t LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE6_AUTHEN_TOG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE6_AUTHEN_TOG &ref() { return *reinterpret_cast<volatile GPR_PRIVATE6_AUTHEN_TOG*>(0x40CC4CDC); }
};

// General Purpose Register
union GPR_PRIVATE7 {
  
  // Bit field definition.
  struct {
    // read-write - GP register
    uint32_t GPR : 32;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE7() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE7 &ref() { return *reinterpret_cast<volatile GPR_PRIVATE7*>(0x40CC4CE0); }
};

// General Purpose Register
union GPR_PRIVATE7_SET {
  
  // Bit field definition.
  struct {
    // read-write - GP register
    uint32_t GPR : 32;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE7_SET() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE7_SET &ref() { return *reinterpret_cast<volatile GPR_PRIVATE7_SET*>(0x40CC4CE4); }
};

// General Purpose Register
union GPR_PRIVATE7_CLR {
  
  // Bit field definition.
  struct {
    // read-write - GP register
    uint32_t GPR : 32;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE7_CLR() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE7_CLR &ref() { return *reinterpret_cast<volatile GPR_PRIVATE7_CLR*>(0x40CC4CE8); }
};

// General Purpose Register
union GPR_PRIVATE7_TOG {
  
  // Bit field definition.
  struct {
    // read-write - GP register
    uint32_t GPR : 32;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE7_TOG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE7_TOG &ref() { return *reinterpret_cast<volatile GPR_PRIVATE7_TOG*>(0x40CC4CEC); }
};

// GPR access control
union GPR_PRIVATE7_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // Clock cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // Clock can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // Lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Whitelist
  enum class eWHITE_LIST : uint32_t {
    // This domain is NOT allowed to change clock.
    eWHITE_LIST_0 = 0,
    // This domain is allowed to change clock.
    eWHITE_LIST_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by Domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does NOT work in Domain Mode.
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode.
    eDOMAIN_MODE_1 = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    eWHITE_LIST WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by Domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    uint32_t _reserved_3 : 3;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE7_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE7_AUTHEN &ref() { return *reinterpret_cast<volatile GPR_PRIVATE7_AUTHEN*>(0x40CC4CF0); }
};

// GPR access control
union GPR_PRIVATE7_AUTHEN_SET {
  
  // Bit field definition.
  struct {
    // read-write - User access
    uint32_t TZ_USER : 1;
    // read-write - Non-secure access
    uint32_t TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    uint32_t LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    uint32_t LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by Domain
    uint32_t DOMAIN_MODE : 1;
    uint32_t _reserved_3 : 3;
    // read-write - Lock low power and access mode
    uint32_t LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE7_AUTHEN_SET() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE7_AUTHEN_SET &ref() { return *reinterpret_cast<volatile GPR_PRIVATE7_AUTHEN_SET*>(0x40CC4CF4); }
};

// GPR access control
union GPR_PRIVATE7_AUTHEN_CLR {
  
  // Bit field definition.
  struct {
    // read-write - User access
    uint32_t TZ_USER : 1;
    // read-write - Non-secure access
    uint32_t TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    uint32_t LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    uint32_t LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by Domain
    uint32_t DOMAIN_MODE : 1;
    uint32_t _reserved_3 : 3;
    // read-write - Lock low power and access mode
    uint32_t LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE7_AUTHEN_CLR() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE7_AUTHEN_CLR &ref() { return *reinterpret_cast<volatile GPR_PRIVATE7_AUTHEN_CLR*>(0x40CC4CF8); }
};

// GPR access control
union GPR_PRIVATE7_AUTHEN_TOG {
  
  // Bit field definition.
  struct {
    // read-write - User access
    uint32_t TZ_USER : 1;
    // read-write - Non-secure access
    uint32_t TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - Lock truszone setting
    uint32_t LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    uint32_t LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by Domain
    uint32_t DOMAIN_MODE : 1;
    uint32_t _reserved_3 : 3;
    // read-write - Lock low power and access mode
    uint32_t LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  GPR_PRIVATE7_AUTHEN_TOG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile GPR_PRIVATE7_AUTHEN_TOG &ref() { return *reinterpret_cast<volatile GPR_PRIVATE7_AUTHEN_TOG*>(0x40CC4CFC); }
};

// LPCG direct control
union LPCG0_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG0_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG0_DIRECT &ref() { return *reinterpret_cast<volatile LPCG0_DIRECT*>(0x40CC6000); }
};

// LPCG domain control
union LPCG0_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG0_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG0_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG0_DOMAIN*>(0x40CC6004); }
};

// LPCG working status
union LPCG0_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG0_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG0_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG0_STATUS0*>(0x40CC6010); }
};

// LPCG low power status
union LPCG0_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG0_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG0_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG0_STATUS1*>(0x40CC6014); }
};

// LPCG configuration
union LPCG0_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG0_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG0_CONFIG &ref() { return *reinterpret_cast<volatile LPCG0_CONFIG*>(0x40CC6018); }
};

// LPCG access control
union LPCG0_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG0_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG0_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG0_AUTHEN*>(0x40CC601C); }
};

// LPCG direct control
union LPCG1_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG1_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG1_DIRECT &ref() { return *reinterpret_cast<volatile LPCG1_DIRECT*>(0x40CC6020); }
};

// LPCG domain control
union LPCG1_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG1_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG1_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG1_DOMAIN*>(0x40CC6024); }
};

// LPCG working status
union LPCG1_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG1_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG1_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG1_STATUS0*>(0x40CC6030); }
};

// LPCG low power status
union LPCG1_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG1_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG1_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG1_STATUS1*>(0x40CC6034); }
};

// LPCG configuration
union LPCG1_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG1_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG1_CONFIG &ref() { return *reinterpret_cast<volatile LPCG1_CONFIG*>(0x40CC6038); }
};

// LPCG access control
union LPCG1_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG1_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG1_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG1_AUTHEN*>(0x40CC603C); }
};

// LPCG direct control
union LPCG2_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG2_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG2_DIRECT &ref() { return *reinterpret_cast<volatile LPCG2_DIRECT*>(0x40CC6040); }
};

// LPCG domain control
union LPCG2_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG2_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG2_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG2_DOMAIN*>(0x40CC6044); }
};

// LPCG Setpoint setting
union LPCG2_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG2_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG2_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG2_SETPOINT*>(0x40CC6048); }
};

// LPCG working status
union LPCG2_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG2_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG2_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG2_STATUS0*>(0x40CC6050); }
};

// LPCG low power status
union LPCG2_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG2_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG2_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG2_STATUS1*>(0x40CC6054); }
};

// LPCG configuration
union LPCG2_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG2_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG2_CONFIG &ref() { return *reinterpret_cast<volatile LPCG2_CONFIG*>(0x40CC6058); }
};

// LPCG access control
union LPCG2_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG2_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG2_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG2_AUTHEN*>(0x40CC605C); }
};

// LPCG direct control
union LPCG3_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG3_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG3_DIRECT &ref() { return *reinterpret_cast<volatile LPCG3_DIRECT*>(0x40CC6060); }
};

// LPCG domain control
union LPCG3_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG3_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG3_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG3_DOMAIN*>(0x40CC6064); }
};

// LPCG Setpoint setting
union LPCG3_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG3_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG3_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG3_SETPOINT*>(0x40CC6068); }
};

// LPCG working status
union LPCG3_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG3_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG3_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG3_STATUS0*>(0x40CC6070); }
};

// LPCG low power status
union LPCG3_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG3_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG3_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG3_STATUS1*>(0x40CC6074); }
};

// LPCG configuration
union LPCG3_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG3_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG3_CONFIG &ref() { return *reinterpret_cast<volatile LPCG3_CONFIG*>(0x40CC6078); }
};

// LPCG access control
union LPCG3_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG3_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG3_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG3_AUTHEN*>(0x40CC607C); }
};

// LPCG direct control
union LPCG4_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG4_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG4_DIRECT &ref() { return *reinterpret_cast<volatile LPCG4_DIRECT*>(0x40CC6080); }
};

// LPCG domain control
union LPCG4_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG4_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG4_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG4_DOMAIN*>(0x40CC6084); }
};

// LPCG Setpoint setting
union LPCG4_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG4_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG4_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG4_SETPOINT*>(0x40CC6088); }
};

// LPCG working status
union LPCG4_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG4_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG4_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG4_STATUS0*>(0x40CC6090); }
};

// LPCG low power status
union LPCG4_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG4_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG4_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG4_STATUS1*>(0x40CC6094); }
};

// LPCG configuration
union LPCG4_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG4_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG4_CONFIG &ref() { return *reinterpret_cast<volatile LPCG4_CONFIG*>(0x40CC6098); }
};

// LPCG access control
union LPCG4_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG4_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG4_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG4_AUTHEN*>(0x40CC609C); }
};

// LPCG direct control
union LPCG5_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG5_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG5_DIRECT &ref() { return *reinterpret_cast<volatile LPCG5_DIRECT*>(0x40CC60A0); }
};

// LPCG domain control
union LPCG5_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG5_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG5_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG5_DOMAIN*>(0x40CC60A4); }
};

// LPCG Setpoint setting
union LPCG5_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG5_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG5_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG5_SETPOINT*>(0x40CC60A8); }
};

// LPCG working status
union LPCG5_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG5_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG5_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG5_STATUS0*>(0x40CC60B0); }
};

// LPCG low power status
union LPCG5_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG5_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG5_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG5_STATUS1*>(0x40CC60B4); }
};

// LPCG configuration
union LPCG5_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG5_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG5_CONFIG &ref() { return *reinterpret_cast<volatile LPCG5_CONFIG*>(0x40CC60B8); }
};

// LPCG access control
union LPCG5_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG5_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG5_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG5_AUTHEN*>(0x40CC60BC); }
};

// LPCG direct control
union LPCG6_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG6_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG6_DIRECT &ref() { return *reinterpret_cast<volatile LPCG6_DIRECT*>(0x40CC60C0); }
};

// LPCG domain control
union LPCG6_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG6_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG6_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG6_DOMAIN*>(0x40CC60C4); }
};

// LPCG Setpoint setting
union LPCG6_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG6_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG6_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG6_SETPOINT*>(0x40CC60C8); }
};

// LPCG working status
union LPCG6_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG6_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG6_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG6_STATUS0*>(0x40CC60D0); }
};

// LPCG low power status
union LPCG6_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG6_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG6_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG6_STATUS1*>(0x40CC60D4); }
};

// LPCG configuration
union LPCG6_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG6_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG6_CONFIG &ref() { return *reinterpret_cast<volatile LPCG6_CONFIG*>(0x40CC60D8); }
};

// LPCG access control
union LPCG6_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG6_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG6_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG6_AUTHEN*>(0x40CC60DC); }
};

// LPCG direct control
union LPCG7_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG7_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG7_DIRECT &ref() { return *reinterpret_cast<volatile LPCG7_DIRECT*>(0x40CC60E0); }
};

// LPCG domain control
union LPCG7_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG7_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG7_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG7_DOMAIN*>(0x40CC60E4); }
};

// LPCG Setpoint setting
union LPCG7_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG7_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG7_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG7_SETPOINT*>(0x40CC60E8); }
};

// LPCG working status
union LPCG7_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG7_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG7_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG7_STATUS0*>(0x40CC60F0); }
};

// LPCG low power status
union LPCG7_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG7_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG7_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG7_STATUS1*>(0x40CC60F4); }
};

// LPCG configuration
union LPCG7_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG7_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG7_CONFIG &ref() { return *reinterpret_cast<volatile LPCG7_CONFIG*>(0x40CC60F8); }
};

// LPCG access control
union LPCG7_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG7_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG7_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG7_AUTHEN*>(0x40CC60FC); }
};

// LPCG direct control
union LPCG8_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG8_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG8_DIRECT &ref() { return *reinterpret_cast<volatile LPCG8_DIRECT*>(0x40CC6100); }
};

// LPCG domain control
union LPCG8_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG8_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG8_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG8_DOMAIN*>(0x40CC6104); }
};

// LPCG Setpoint setting
union LPCG8_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG8_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG8_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG8_SETPOINT*>(0x40CC6108); }
};

// LPCG working status
union LPCG8_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG8_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG8_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG8_STATUS0*>(0x40CC6110); }
};

// LPCG low power status
union LPCG8_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG8_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG8_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG8_STATUS1*>(0x40CC6114); }
};

// LPCG configuration
union LPCG8_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG8_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG8_CONFIG &ref() { return *reinterpret_cast<volatile LPCG8_CONFIG*>(0x40CC6118); }
};

// LPCG access control
union LPCG8_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG8_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG8_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG8_AUTHEN*>(0x40CC611C); }
};

// LPCG direct control
union LPCG9_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG9_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG9_DIRECT &ref() { return *reinterpret_cast<volatile LPCG9_DIRECT*>(0x40CC6120); }
};

// LPCG domain control
union LPCG9_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG9_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG9_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG9_DOMAIN*>(0x40CC6124); }
};

// LPCG Setpoint setting
union LPCG9_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG9_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG9_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG9_SETPOINT*>(0x40CC6128); }
};

// LPCG working status
union LPCG9_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG9_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG9_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG9_STATUS0*>(0x40CC6130); }
};

// LPCG low power status
union LPCG9_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG9_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG9_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG9_STATUS1*>(0x40CC6134); }
};

// LPCG configuration
union LPCG9_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG9_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG9_CONFIG &ref() { return *reinterpret_cast<volatile LPCG9_CONFIG*>(0x40CC6138); }
};

// LPCG access control
union LPCG9_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG9_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG9_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG9_AUTHEN*>(0x40CC613C); }
};

// LPCG direct control
union LPCG10_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG10_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG10_DIRECT &ref() { return *reinterpret_cast<volatile LPCG10_DIRECT*>(0x40CC6140); }
};

// LPCG domain control
union LPCG10_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG10_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG10_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG10_DOMAIN*>(0x40CC6144); }
};

// LPCG Setpoint setting
union LPCG10_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG10_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG10_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG10_SETPOINT*>(0x40CC6148); }
};

// LPCG working status
union LPCG10_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG10_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG10_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG10_STATUS0*>(0x40CC6150); }
};

// LPCG low power status
union LPCG10_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG10_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG10_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG10_STATUS1*>(0x40CC6154); }
};

// LPCG configuration
union LPCG10_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG10_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG10_CONFIG &ref() { return *reinterpret_cast<volatile LPCG10_CONFIG*>(0x40CC6158); }
};

// LPCG access control
union LPCG10_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG10_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG10_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG10_AUTHEN*>(0x40CC615C); }
};

// LPCG direct control
union LPCG11_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG11_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG11_DIRECT &ref() { return *reinterpret_cast<volatile LPCG11_DIRECT*>(0x40CC6160); }
};

// LPCG domain control
union LPCG11_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG11_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG11_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG11_DOMAIN*>(0x40CC6164); }
};

// LPCG Setpoint setting
union LPCG11_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG11_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG11_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG11_SETPOINT*>(0x40CC6168); }
};

// LPCG working status
union LPCG11_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG11_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG11_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG11_STATUS0*>(0x40CC6170); }
};

// LPCG low power status
union LPCG11_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG11_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG11_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG11_STATUS1*>(0x40CC6174); }
};

// LPCG configuration
union LPCG11_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG11_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG11_CONFIG &ref() { return *reinterpret_cast<volatile LPCG11_CONFIG*>(0x40CC6178); }
};

// LPCG access control
union LPCG11_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG11_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG11_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG11_AUTHEN*>(0x40CC617C); }
};

// LPCG direct control
union LPCG12_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG12_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG12_DIRECT &ref() { return *reinterpret_cast<volatile LPCG12_DIRECT*>(0x40CC6180); }
};

// LPCG domain control
union LPCG12_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG12_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG12_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG12_DOMAIN*>(0x40CC6184); }
};

// LPCG Setpoint setting
union LPCG12_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG12_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG12_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG12_SETPOINT*>(0x40CC6188); }
};

// LPCG working status
union LPCG12_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG12_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG12_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG12_STATUS0*>(0x40CC6190); }
};

// LPCG low power status
union LPCG12_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG12_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG12_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG12_STATUS1*>(0x40CC6194); }
};

// LPCG configuration
union LPCG12_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG12_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG12_CONFIG &ref() { return *reinterpret_cast<volatile LPCG12_CONFIG*>(0x40CC6198); }
};

// LPCG access control
union LPCG12_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG12_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG12_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG12_AUTHEN*>(0x40CC619C); }
};

// LPCG direct control
union LPCG13_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG13_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG13_DIRECT &ref() { return *reinterpret_cast<volatile LPCG13_DIRECT*>(0x40CC61A0); }
};

// LPCG domain control
union LPCG13_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG13_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG13_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG13_DOMAIN*>(0x40CC61A4); }
};

// LPCG working status
union LPCG13_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG13_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG13_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG13_STATUS0*>(0x40CC61B0); }
};

// LPCG low power status
union LPCG13_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG13_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG13_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG13_STATUS1*>(0x40CC61B4); }
};

// LPCG configuration
union LPCG13_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG13_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG13_CONFIG &ref() { return *reinterpret_cast<volatile LPCG13_CONFIG*>(0x40CC61B8); }
};

// LPCG access control
union LPCG13_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG13_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG13_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG13_AUTHEN*>(0x40CC61BC); }
};

// LPCG direct control
union LPCG14_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG14_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG14_DIRECT &ref() { return *reinterpret_cast<volatile LPCG14_DIRECT*>(0x40CC61C0); }
};

// LPCG domain control
union LPCG14_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG14_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG14_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG14_DOMAIN*>(0x40CC61C4); }
};

// LPCG Setpoint setting
union LPCG14_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG14_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG14_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG14_SETPOINT*>(0x40CC61C8); }
};

// LPCG working status
union LPCG14_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG14_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG14_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG14_STATUS0*>(0x40CC61D0); }
};

// LPCG low power status
union LPCG14_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG14_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG14_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG14_STATUS1*>(0x40CC61D4); }
};

// LPCG configuration
union LPCG14_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG14_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG14_CONFIG &ref() { return *reinterpret_cast<volatile LPCG14_CONFIG*>(0x40CC61D8); }
};

// LPCG access control
union LPCG14_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG14_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG14_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG14_AUTHEN*>(0x40CC61DC); }
};

// LPCG direct control
union LPCG15_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG15_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG15_DIRECT &ref() { return *reinterpret_cast<volatile LPCG15_DIRECT*>(0x40CC61E0); }
};

// LPCG domain control
union LPCG15_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG15_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG15_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG15_DOMAIN*>(0x40CC61E4); }
};

// LPCG Setpoint setting
union LPCG15_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG15_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG15_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG15_SETPOINT*>(0x40CC61E8); }
};

// LPCG working status
union LPCG15_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG15_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG15_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG15_STATUS0*>(0x40CC61F0); }
};

// LPCG low power status
union LPCG15_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG15_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG15_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG15_STATUS1*>(0x40CC61F4); }
};

// LPCG configuration
union LPCG15_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG15_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG15_CONFIG &ref() { return *reinterpret_cast<volatile LPCG15_CONFIG*>(0x40CC61F8); }
};

// LPCG access control
union LPCG15_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG15_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG15_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG15_AUTHEN*>(0x40CC61FC); }
};

// LPCG direct control
union LPCG16_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG16_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG16_DIRECT &ref() { return *reinterpret_cast<volatile LPCG16_DIRECT*>(0x40CC6200); }
};

// LPCG domain control
union LPCG16_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG16_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG16_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG16_DOMAIN*>(0x40CC6204); }
};

// LPCG Setpoint setting
union LPCG16_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG16_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG16_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG16_SETPOINT*>(0x40CC6208); }
};

// LPCG working status
union LPCG16_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG16_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG16_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG16_STATUS0*>(0x40CC6210); }
};

// LPCG low power status
union LPCG16_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG16_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG16_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG16_STATUS1*>(0x40CC6214); }
};

// LPCG configuration
union LPCG16_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG16_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG16_CONFIG &ref() { return *reinterpret_cast<volatile LPCG16_CONFIG*>(0x40CC6218); }
};

// LPCG access control
union LPCG16_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG16_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG16_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG16_AUTHEN*>(0x40CC621C); }
};

// LPCG direct control
union LPCG17_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG17_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG17_DIRECT &ref() { return *reinterpret_cast<volatile LPCG17_DIRECT*>(0x40CC6220); }
};

// LPCG domain control
union LPCG17_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG17_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG17_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG17_DOMAIN*>(0x40CC6224); }
};

// LPCG Setpoint setting
union LPCG17_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG17_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG17_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG17_SETPOINT*>(0x40CC6228); }
};

// LPCG working status
union LPCG17_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG17_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG17_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG17_STATUS0*>(0x40CC6230); }
};

// LPCG low power status
union LPCG17_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG17_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG17_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG17_STATUS1*>(0x40CC6234); }
};

// LPCG configuration
union LPCG17_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG17_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG17_CONFIG &ref() { return *reinterpret_cast<volatile LPCG17_CONFIG*>(0x40CC6238); }
};

// LPCG access control
union LPCG17_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG17_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG17_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG17_AUTHEN*>(0x40CC623C); }
};

// LPCG direct control
union LPCG18_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG18_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG18_DIRECT &ref() { return *reinterpret_cast<volatile LPCG18_DIRECT*>(0x40CC6240); }
};

// LPCG domain control
union LPCG18_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG18_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG18_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG18_DOMAIN*>(0x40CC6244); }
};

// LPCG Setpoint setting
union LPCG18_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG18_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG18_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG18_SETPOINT*>(0x40CC6248); }
};

// LPCG working status
union LPCG18_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG18_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG18_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG18_STATUS0*>(0x40CC6250); }
};

// LPCG low power status
union LPCG18_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG18_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG18_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG18_STATUS1*>(0x40CC6254); }
};

// LPCG configuration
union LPCG18_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG18_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG18_CONFIG &ref() { return *reinterpret_cast<volatile LPCG18_CONFIG*>(0x40CC6258); }
};

// LPCG access control
union LPCG18_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG18_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG18_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG18_AUTHEN*>(0x40CC625C); }
};

// LPCG direct control
union LPCG19_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG19_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG19_DIRECT &ref() { return *reinterpret_cast<volatile LPCG19_DIRECT*>(0x40CC6260); }
};

// LPCG domain control
union LPCG19_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG19_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG19_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG19_DOMAIN*>(0x40CC6264); }
};

// LPCG Setpoint setting
union LPCG19_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG19_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG19_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG19_SETPOINT*>(0x40CC6268); }
};

// LPCG working status
union LPCG19_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG19_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG19_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG19_STATUS0*>(0x40CC6270); }
};

// LPCG low power status
union LPCG19_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG19_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG19_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG19_STATUS1*>(0x40CC6274); }
};

// LPCG configuration
union LPCG19_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG19_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG19_CONFIG &ref() { return *reinterpret_cast<volatile LPCG19_CONFIG*>(0x40CC6278); }
};

// LPCG access control
union LPCG19_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG19_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG19_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG19_AUTHEN*>(0x40CC627C); }
};

// LPCG direct control
union LPCG20_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG20_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG20_DIRECT &ref() { return *reinterpret_cast<volatile LPCG20_DIRECT*>(0x40CC6280); }
};

// LPCG domain control
union LPCG20_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG20_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG20_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG20_DOMAIN*>(0x40CC6284); }
};

// LPCG working status
union LPCG20_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG20_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG20_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG20_STATUS0*>(0x40CC6290); }
};

// LPCG low power status
union LPCG20_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG20_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG20_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG20_STATUS1*>(0x40CC6294); }
};

// LPCG configuration
union LPCG20_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG20_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG20_CONFIG &ref() { return *reinterpret_cast<volatile LPCG20_CONFIG*>(0x40CC6298); }
};

// LPCG access control
union LPCG20_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG20_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG20_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG20_AUTHEN*>(0x40CC629C); }
};

// LPCG direct control
union LPCG21_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG21_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG21_DIRECT &ref() { return *reinterpret_cast<volatile LPCG21_DIRECT*>(0x40CC62A0); }
};

// LPCG domain control
union LPCG21_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG21_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG21_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG21_DOMAIN*>(0x40CC62A4); }
};

// LPCG working status
union LPCG21_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG21_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG21_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG21_STATUS0*>(0x40CC62B0); }
};

// LPCG low power status
union LPCG21_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG21_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG21_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG21_STATUS1*>(0x40CC62B4); }
};

// LPCG configuration
union LPCG21_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG21_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG21_CONFIG &ref() { return *reinterpret_cast<volatile LPCG21_CONFIG*>(0x40CC62B8); }
};

// LPCG access control
union LPCG21_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG21_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG21_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG21_AUTHEN*>(0x40CC62BC); }
};

// LPCG direct control
union LPCG22_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG22_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG22_DIRECT &ref() { return *reinterpret_cast<volatile LPCG22_DIRECT*>(0x40CC62C0); }
};

// LPCG domain control
union LPCG22_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG22_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG22_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG22_DOMAIN*>(0x40CC62C4); }
};

// LPCG working status
union LPCG22_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG22_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG22_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG22_STATUS0*>(0x40CC62D0); }
};

// LPCG low power status
union LPCG22_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG22_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG22_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG22_STATUS1*>(0x40CC62D4); }
};

// LPCG configuration
union LPCG22_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG22_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG22_CONFIG &ref() { return *reinterpret_cast<volatile LPCG22_CONFIG*>(0x40CC62D8); }
};

// LPCG access control
union LPCG22_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG22_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG22_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG22_AUTHEN*>(0x40CC62DC); }
};

// LPCG direct control
union LPCG23_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG23_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG23_DIRECT &ref() { return *reinterpret_cast<volatile LPCG23_DIRECT*>(0x40CC62E0); }
};

// LPCG domain control
union LPCG23_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG23_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG23_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG23_DOMAIN*>(0x40CC62E4); }
};

// LPCG working status
union LPCG23_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG23_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG23_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG23_STATUS0*>(0x40CC62F0); }
};

// LPCG low power status
union LPCG23_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG23_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG23_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG23_STATUS1*>(0x40CC62F4); }
};

// LPCG configuration
union LPCG23_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG23_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG23_CONFIG &ref() { return *reinterpret_cast<volatile LPCG23_CONFIG*>(0x40CC62F8); }
};

// LPCG access control
union LPCG23_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG23_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG23_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG23_AUTHEN*>(0x40CC62FC); }
};

// LPCG direct control
union LPCG24_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG24_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG24_DIRECT &ref() { return *reinterpret_cast<volatile LPCG24_DIRECT*>(0x40CC6300); }
};

// LPCG domain control
union LPCG24_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG24_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG24_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG24_DOMAIN*>(0x40CC6304); }
};

// LPCG Setpoint setting
union LPCG24_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG24_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG24_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG24_SETPOINT*>(0x40CC6308); }
};

// LPCG working status
union LPCG24_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG24_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG24_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG24_STATUS0*>(0x40CC6310); }
};

// LPCG low power status
union LPCG24_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG24_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG24_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG24_STATUS1*>(0x40CC6314); }
};

// LPCG configuration
union LPCG24_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG24_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG24_CONFIG &ref() { return *reinterpret_cast<volatile LPCG24_CONFIG*>(0x40CC6318); }
};

// LPCG access control
union LPCG24_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG24_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG24_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG24_AUTHEN*>(0x40CC631C); }
};

// LPCG direct control
union LPCG25_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG25_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG25_DIRECT &ref() { return *reinterpret_cast<volatile LPCG25_DIRECT*>(0x40CC6320); }
};

// LPCG domain control
union LPCG25_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG25_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG25_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG25_DOMAIN*>(0x40CC6324); }
};

// LPCG Setpoint setting
union LPCG25_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG25_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG25_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG25_SETPOINT*>(0x40CC6328); }
};

// LPCG working status
union LPCG25_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG25_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG25_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG25_STATUS0*>(0x40CC6330); }
};

// LPCG low power status
union LPCG25_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG25_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG25_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG25_STATUS1*>(0x40CC6334); }
};

// LPCG configuration
union LPCG25_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG25_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG25_CONFIG &ref() { return *reinterpret_cast<volatile LPCG25_CONFIG*>(0x40CC6338); }
};

// LPCG access control
union LPCG25_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG25_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG25_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG25_AUTHEN*>(0x40CC633C); }
};

// LPCG direct control
union LPCG26_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG26_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG26_DIRECT &ref() { return *reinterpret_cast<volatile LPCG26_DIRECT*>(0x40CC6340); }
};

// LPCG domain control
union LPCG26_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG26_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG26_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG26_DOMAIN*>(0x40CC6344); }
};

// LPCG Setpoint setting
union LPCG26_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG26_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG26_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG26_SETPOINT*>(0x40CC6348); }
};

// LPCG working status
union LPCG26_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG26_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG26_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG26_STATUS0*>(0x40CC6350); }
};

// LPCG low power status
union LPCG26_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG26_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG26_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG26_STATUS1*>(0x40CC6354); }
};

// LPCG configuration
union LPCG26_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG26_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG26_CONFIG &ref() { return *reinterpret_cast<volatile LPCG26_CONFIG*>(0x40CC6358); }
};

// LPCG access control
union LPCG26_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG26_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG26_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG26_AUTHEN*>(0x40CC635C); }
};

// LPCG direct control
union LPCG27_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG27_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG27_DIRECT &ref() { return *reinterpret_cast<volatile LPCG27_DIRECT*>(0x40CC6360); }
};

// LPCG domain control
union LPCG27_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG27_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG27_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG27_DOMAIN*>(0x40CC6364); }
};

// LPCG Setpoint setting
union LPCG27_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG27_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG27_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG27_SETPOINT*>(0x40CC6368); }
};

// LPCG working status
union LPCG27_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG27_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG27_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG27_STATUS0*>(0x40CC6370); }
};

// LPCG low power status
union LPCG27_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG27_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG27_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG27_STATUS1*>(0x40CC6374); }
};

// LPCG configuration
union LPCG27_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG27_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG27_CONFIG &ref() { return *reinterpret_cast<volatile LPCG27_CONFIG*>(0x40CC6378); }
};

// LPCG access control
union LPCG27_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG27_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG27_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG27_AUTHEN*>(0x40CC637C); }
};

// LPCG direct control
union LPCG28_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG28_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG28_DIRECT &ref() { return *reinterpret_cast<volatile LPCG28_DIRECT*>(0x40CC6380); }
};

// LPCG domain control
union LPCG28_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG28_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG28_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG28_DOMAIN*>(0x40CC6384); }
};

// LPCG Setpoint setting
union LPCG28_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG28_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG28_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG28_SETPOINT*>(0x40CC6388); }
};

// LPCG working status
union LPCG28_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG28_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG28_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG28_STATUS0*>(0x40CC6390); }
};

// LPCG low power status
union LPCG28_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG28_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG28_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG28_STATUS1*>(0x40CC6394); }
};

// LPCG configuration
union LPCG28_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG28_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG28_CONFIG &ref() { return *reinterpret_cast<volatile LPCG28_CONFIG*>(0x40CC6398); }
};

// LPCG access control
union LPCG28_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG28_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG28_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG28_AUTHEN*>(0x40CC639C); }
};

// LPCG direct control
union LPCG29_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG29_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG29_DIRECT &ref() { return *reinterpret_cast<volatile LPCG29_DIRECT*>(0x40CC63A0); }
};

// LPCG domain control
union LPCG29_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG29_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG29_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG29_DOMAIN*>(0x40CC63A4); }
};

// LPCG Setpoint setting
union LPCG29_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG29_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG29_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG29_SETPOINT*>(0x40CC63A8); }
};

// LPCG working status
union LPCG29_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG29_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG29_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG29_STATUS0*>(0x40CC63B0); }
};

// LPCG low power status
union LPCG29_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG29_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG29_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG29_STATUS1*>(0x40CC63B4); }
};

// LPCG configuration
union LPCG29_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG29_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG29_CONFIG &ref() { return *reinterpret_cast<volatile LPCG29_CONFIG*>(0x40CC63B8); }
};

// LPCG access control
union LPCG29_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG29_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG29_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG29_AUTHEN*>(0x40CC63BC); }
};

// LPCG direct control
union LPCG30_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG30_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG30_DIRECT &ref() { return *reinterpret_cast<volatile LPCG30_DIRECT*>(0x40CC63C0); }
};

// LPCG domain control
union LPCG30_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG30_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG30_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG30_DOMAIN*>(0x40CC63C4); }
};

// LPCG Setpoint setting
union LPCG30_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG30_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG30_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG30_SETPOINT*>(0x40CC63C8); }
};

// LPCG working status
union LPCG30_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG30_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG30_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG30_STATUS0*>(0x40CC63D0); }
};

// LPCG low power status
union LPCG30_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG30_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG30_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG30_STATUS1*>(0x40CC63D4); }
};

// LPCG configuration
union LPCG30_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG30_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG30_CONFIG &ref() { return *reinterpret_cast<volatile LPCG30_CONFIG*>(0x40CC63D8); }
};

// LPCG access control
union LPCG30_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG30_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG30_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG30_AUTHEN*>(0x40CC63DC); }
};

// LPCG direct control
union LPCG31_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG31_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG31_DIRECT &ref() { return *reinterpret_cast<volatile LPCG31_DIRECT*>(0x40CC63E0); }
};

// LPCG domain control
union LPCG31_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG31_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG31_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG31_DOMAIN*>(0x40CC63E4); }
};

// LPCG Setpoint setting
union LPCG31_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG31_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG31_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG31_SETPOINT*>(0x40CC63E8); }
};

// LPCG working status
union LPCG31_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG31_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG31_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG31_STATUS0*>(0x40CC63F0); }
};

// LPCG low power status
union LPCG31_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG31_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG31_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG31_STATUS1*>(0x40CC63F4); }
};

// LPCG configuration
union LPCG31_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG31_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG31_CONFIG &ref() { return *reinterpret_cast<volatile LPCG31_CONFIG*>(0x40CC63F8); }
};

// LPCG access control
union LPCG31_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG31_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG31_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG31_AUTHEN*>(0x40CC63FC); }
};

// LPCG direct control
union LPCG32_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG32_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG32_DIRECT &ref() { return *reinterpret_cast<volatile LPCG32_DIRECT*>(0x40CC6400); }
};

// LPCG domain control
union LPCG32_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG32_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG32_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG32_DOMAIN*>(0x40CC6404); }
};

// LPCG Setpoint setting
union LPCG32_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG32_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG32_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG32_SETPOINT*>(0x40CC6408); }
};

// LPCG working status
union LPCG32_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG32_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG32_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG32_STATUS0*>(0x40CC6410); }
};

// LPCG low power status
union LPCG32_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG32_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG32_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG32_STATUS1*>(0x40CC6414); }
};

// LPCG configuration
union LPCG32_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG32_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG32_CONFIG &ref() { return *reinterpret_cast<volatile LPCG32_CONFIG*>(0x40CC6418); }
};

// LPCG access control
union LPCG32_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG32_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG32_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG32_AUTHEN*>(0x40CC641C); }
};

// LPCG direct control
union LPCG33_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG33_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG33_DIRECT &ref() { return *reinterpret_cast<volatile LPCG33_DIRECT*>(0x40CC6420); }
};

// LPCG domain control
union LPCG33_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG33_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG33_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG33_DOMAIN*>(0x40CC6424); }
};

// LPCG Setpoint setting
union LPCG33_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG33_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG33_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG33_SETPOINT*>(0x40CC6428); }
};

// LPCG working status
union LPCG33_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG33_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG33_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG33_STATUS0*>(0x40CC6430); }
};

// LPCG low power status
union LPCG33_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG33_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG33_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG33_STATUS1*>(0x40CC6434); }
};

// LPCG configuration
union LPCG33_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG33_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG33_CONFIG &ref() { return *reinterpret_cast<volatile LPCG33_CONFIG*>(0x40CC6438); }
};

// LPCG access control
union LPCG33_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG33_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG33_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG33_AUTHEN*>(0x40CC643C); }
};

// LPCG direct control
union LPCG34_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG34_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG34_DIRECT &ref() { return *reinterpret_cast<volatile LPCG34_DIRECT*>(0x40CC6440); }
};

// LPCG domain control
union LPCG34_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG34_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG34_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG34_DOMAIN*>(0x40CC6444); }
};

// LPCG Setpoint setting
union LPCG34_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG34_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG34_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG34_SETPOINT*>(0x40CC6448); }
};

// LPCG working status
union LPCG34_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG34_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG34_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG34_STATUS0*>(0x40CC6450); }
};

// LPCG low power status
union LPCG34_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG34_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG34_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG34_STATUS1*>(0x40CC6454); }
};

// LPCG configuration
union LPCG34_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG34_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG34_CONFIG &ref() { return *reinterpret_cast<volatile LPCG34_CONFIG*>(0x40CC6458); }
};

// LPCG access control
union LPCG34_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG34_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG34_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG34_AUTHEN*>(0x40CC645C); }
};

// LPCG direct control
union LPCG35_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG35_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG35_DIRECT &ref() { return *reinterpret_cast<volatile LPCG35_DIRECT*>(0x40CC6460); }
};

// LPCG domain control
union LPCG35_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG35_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG35_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG35_DOMAIN*>(0x40CC6464); }
};

// LPCG Setpoint setting
union LPCG35_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG35_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG35_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG35_SETPOINT*>(0x40CC6468); }
};

// LPCG working status
union LPCG35_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG35_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG35_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG35_STATUS0*>(0x40CC6470); }
};

// LPCG low power status
union LPCG35_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG35_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG35_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG35_STATUS1*>(0x40CC6474); }
};

// LPCG configuration
union LPCG35_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG35_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG35_CONFIG &ref() { return *reinterpret_cast<volatile LPCG35_CONFIG*>(0x40CC6478); }
};

// LPCG access control
union LPCG35_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG35_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG35_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG35_AUTHEN*>(0x40CC647C); }
};

// LPCG direct control
union LPCG36_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG36_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG36_DIRECT &ref() { return *reinterpret_cast<volatile LPCG36_DIRECT*>(0x40CC6480); }
};

// LPCG domain control
union LPCG36_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG36_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG36_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG36_DOMAIN*>(0x40CC6484); }
};

// LPCG Setpoint setting
union LPCG36_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG36_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG36_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG36_SETPOINT*>(0x40CC6488); }
};

// LPCG working status
union LPCG36_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG36_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG36_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG36_STATUS0*>(0x40CC6490); }
};

// LPCG low power status
union LPCG36_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG36_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG36_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG36_STATUS1*>(0x40CC6494); }
};

// LPCG configuration
union LPCG36_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG36_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG36_CONFIG &ref() { return *reinterpret_cast<volatile LPCG36_CONFIG*>(0x40CC6498); }
};

// LPCG access control
union LPCG36_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG36_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG36_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG36_AUTHEN*>(0x40CC649C); }
};

// LPCG direct control
union LPCG37_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG37_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG37_DIRECT &ref() { return *reinterpret_cast<volatile LPCG37_DIRECT*>(0x40CC64A0); }
};

// LPCG domain control
union LPCG37_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG37_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG37_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG37_DOMAIN*>(0x40CC64A4); }
};

// LPCG Setpoint setting
union LPCG37_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG37_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG37_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG37_SETPOINT*>(0x40CC64A8); }
};

// LPCG working status
union LPCG37_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG37_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG37_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG37_STATUS0*>(0x40CC64B0); }
};

// LPCG low power status
union LPCG37_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG37_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG37_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG37_STATUS1*>(0x40CC64B4); }
};

// LPCG configuration
union LPCG37_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG37_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG37_CONFIG &ref() { return *reinterpret_cast<volatile LPCG37_CONFIG*>(0x40CC64B8); }
};

// LPCG access control
union LPCG37_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG37_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG37_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG37_AUTHEN*>(0x40CC64BC); }
};

// LPCG direct control
union LPCG38_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG38_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG38_DIRECT &ref() { return *reinterpret_cast<volatile LPCG38_DIRECT*>(0x40CC64C0); }
};

// LPCG domain control
union LPCG38_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG38_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG38_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG38_DOMAIN*>(0x40CC64C4); }
};

// LPCG Setpoint setting
union LPCG38_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG38_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG38_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG38_SETPOINT*>(0x40CC64C8); }
};

// LPCG working status
union LPCG38_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG38_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG38_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG38_STATUS0*>(0x40CC64D0); }
};

// LPCG low power status
union LPCG38_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG38_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG38_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG38_STATUS1*>(0x40CC64D4); }
};

// LPCG configuration
union LPCG38_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG38_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG38_CONFIG &ref() { return *reinterpret_cast<volatile LPCG38_CONFIG*>(0x40CC64D8); }
};

// LPCG access control
union LPCG38_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG38_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG38_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG38_AUTHEN*>(0x40CC64DC); }
};

// LPCG direct control
union LPCG39_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG39_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG39_DIRECT &ref() { return *reinterpret_cast<volatile LPCG39_DIRECT*>(0x40CC64E0); }
};

// LPCG domain control
union LPCG39_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG39_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG39_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG39_DOMAIN*>(0x40CC64E4); }
};

// LPCG Setpoint setting
union LPCG39_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG39_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG39_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG39_SETPOINT*>(0x40CC64E8); }
};

// LPCG working status
union LPCG39_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG39_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG39_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG39_STATUS0*>(0x40CC64F0); }
};

// LPCG low power status
union LPCG39_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG39_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG39_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG39_STATUS1*>(0x40CC64F4); }
};

// LPCG configuration
union LPCG39_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG39_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG39_CONFIG &ref() { return *reinterpret_cast<volatile LPCG39_CONFIG*>(0x40CC64F8); }
};

// LPCG access control
union LPCG39_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG39_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG39_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG39_AUTHEN*>(0x40CC64FC); }
};

// LPCG direct control
union LPCG40_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG40_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG40_DIRECT &ref() { return *reinterpret_cast<volatile LPCG40_DIRECT*>(0x40CC6500); }
};

// LPCG domain control
union LPCG40_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG40_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG40_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG40_DOMAIN*>(0x40CC6504); }
};

// LPCG Setpoint setting
union LPCG40_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG40_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG40_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG40_SETPOINT*>(0x40CC6508); }
};

// LPCG working status
union LPCG40_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG40_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG40_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG40_STATUS0*>(0x40CC6510); }
};

// LPCG low power status
union LPCG40_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG40_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG40_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG40_STATUS1*>(0x40CC6514); }
};

// LPCG configuration
union LPCG40_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG40_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG40_CONFIG &ref() { return *reinterpret_cast<volatile LPCG40_CONFIG*>(0x40CC6518); }
};

// LPCG access control
union LPCG40_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG40_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG40_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG40_AUTHEN*>(0x40CC651C); }
};

// LPCG direct control
union LPCG41_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG41_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG41_DIRECT &ref() { return *reinterpret_cast<volatile LPCG41_DIRECT*>(0x40CC6520); }
};

// LPCG domain control
union LPCG41_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG41_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG41_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG41_DOMAIN*>(0x40CC6524); }
};

// LPCG working status
union LPCG41_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG41_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG41_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG41_STATUS0*>(0x40CC6530); }
};

// LPCG low power status
union LPCG41_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG41_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG41_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG41_STATUS1*>(0x40CC6534); }
};

// LPCG configuration
union LPCG41_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG41_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG41_CONFIG &ref() { return *reinterpret_cast<volatile LPCG41_CONFIG*>(0x40CC6538); }
};

// LPCG access control
union LPCG41_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG41_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG41_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG41_AUTHEN*>(0x40CC653C); }
};

// LPCG direct control
union LPCG42_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG42_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG42_DIRECT &ref() { return *reinterpret_cast<volatile LPCG42_DIRECT*>(0x40CC6540); }
};

// LPCG domain control
union LPCG42_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG42_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG42_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG42_DOMAIN*>(0x40CC6544); }
};

// LPCG working status
union LPCG42_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG42_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG42_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG42_STATUS0*>(0x40CC6550); }
};

// LPCG low power status
union LPCG42_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG42_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG42_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG42_STATUS1*>(0x40CC6554); }
};

// LPCG configuration
union LPCG42_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG42_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG42_CONFIG &ref() { return *reinterpret_cast<volatile LPCG42_CONFIG*>(0x40CC6558); }
};

// LPCG access control
union LPCG42_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG42_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG42_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG42_AUTHEN*>(0x40CC655C); }
};

// LPCG direct control
union LPCG43_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG43_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG43_DIRECT &ref() { return *reinterpret_cast<volatile LPCG43_DIRECT*>(0x40CC6560); }
};

// LPCG domain control
union LPCG43_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG43_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG43_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG43_DOMAIN*>(0x40CC6564); }
};

// LPCG Setpoint setting
union LPCG43_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG43_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG43_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG43_SETPOINT*>(0x40CC6568); }
};

// LPCG working status
union LPCG43_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG43_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG43_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG43_STATUS0*>(0x40CC6570); }
};

// LPCG low power status
union LPCG43_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG43_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG43_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG43_STATUS1*>(0x40CC6574); }
};

// LPCG configuration
union LPCG43_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG43_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG43_CONFIG &ref() { return *reinterpret_cast<volatile LPCG43_CONFIG*>(0x40CC6578); }
};

// LPCG access control
union LPCG43_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG43_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG43_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG43_AUTHEN*>(0x40CC657C); }
};

// LPCG direct control
union LPCG44_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG44_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG44_DIRECT &ref() { return *reinterpret_cast<volatile LPCG44_DIRECT*>(0x40CC6580); }
};

// LPCG domain control
union LPCG44_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG44_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG44_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG44_DOMAIN*>(0x40CC6584); }
};

// LPCG Setpoint setting
union LPCG44_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG44_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG44_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG44_SETPOINT*>(0x40CC6588); }
};

// LPCG working status
union LPCG44_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG44_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG44_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG44_STATUS0*>(0x40CC6590); }
};

// LPCG low power status
union LPCG44_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG44_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG44_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG44_STATUS1*>(0x40CC6594); }
};

// LPCG configuration
union LPCG44_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG44_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG44_CONFIG &ref() { return *reinterpret_cast<volatile LPCG44_CONFIG*>(0x40CC6598); }
};

// LPCG access control
union LPCG44_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG44_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG44_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG44_AUTHEN*>(0x40CC659C); }
};

// LPCG direct control
union LPCG45_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG45_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG45_DIRECT &ref() { return *reinterpret_cast<volatile LPCG45_DIRECT*>(0x40CC65A0); }
};

// LPCG domain control
union LPCG45_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG45_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG45_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG45_DOMAIN*>(0x40CC65A4); }
};

// LPCG Setpoint setting
union LPCG45_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG45_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG45_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG45_SETPOINT*>(0x40CC65A8); }
};

// LPCG working status
union LPCG45_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG45_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG45_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG45_STATUS0*>(0x40CC65B0); }
};

// LPCG low power status
union LPCG45_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG45_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG45_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG45_STATUS1*>(0x40CC65B4); }
};

// LPCG configuration
union LPCG45_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG45_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG45_CONFIG &ref() { return *reinterpret_cast<volatile LPCG45_CONFIG*>(0x40CC65B8); }
};

// LPCG access control
union LPCG45_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG45_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG45_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG45_AUTHEN*>(0x40CC65BC); }
};

// LPCG direct control
union LPCG46_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG46_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG46_DIRECT &ref() { return *reinterpret_cast<volatile LPCG46_DIRECT*>(0x40CC65C0); }
};

// LPCG domain control
union LPCG46_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG46_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG46_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG46_DOMAIN*>(0x40CC65C4); }
};

// LPCG Setpoint setting
union LPCG46_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG46_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG46_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG46_SETPOINT*>(0x40CC65C8); }
};

// LPCG working status
union LPCG46_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG46_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG46_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG46_STATUS0*>(0x40CC65D0); }
};

// LPCG low power status
union LPCG46_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG46_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG46_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG46_STATUS1*>(0x40CC65D4); }
};

// LPCG configuration
union LPCG46_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG46_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG46_CONFIG &ref() { return *reinterpret_cast<volatile LPCG46_CONFIG*>(0x40CC65D8); }
};

// LPCG access control
union LPCG46_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG46_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG46_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG46_AUTHEN*>(0x40CC65DC); }
};

// LPCG direct control
union LPCG47_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG47_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG47_DIRECT &ref() { return *reinterpret_cast<volatile LPCG47_DIRECT*>(0x40CC65E0); }
};

// LPCG domain control
union LPCG47_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG47_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG47_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG47_DOMAIN*>(0x40CC65E4); }
};

// LPCG Setpoint setting
union LPCG47_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG47_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG47_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG47_SETPOINT*>(0x40CC65E8); }
};

// LPCG working status
union LPCG47_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG47_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG47_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG47_STATUS0*>(0x40CC65F0); }
};

// LPCG low power status
union LPCG47_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG47_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG47_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG47_STATUS1*>(0x40CC65F4); }
};

// LPCG configuration
union LPCG47_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG47_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG47_CONFIG &ref() { return *reinterpret_cast<volatile LPCG47_CONFIG*>(0x40CC65F8); }
};

// LPCG access control
union LPCG47_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG47_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG47_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG47_AUTHEN*>(0x40CC65FC); }
};

// LPCG direct control
union LPCG48_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG48_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG48_DIRECT &ref() { return *reinterpret_cast<volatile LPCG48_DIRECT*>(0x40CC6600); }
};

// LPCG domain control
union LPCG48_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG48_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG48_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG48_DOMAIN*>(0x40CC6604); }
};

// LPCG Setpoint setting
union LPCG48_SETPOINT {
  
  // Bit field definition.
  struct {
    // read-write - Setpoints
    uint32_t SETPOINT : 16;
    // read-write - Standby
    uint32_t STANDBY : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG48_SETPOINT() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG48_SETPOINT &ref() { return *reinterpret_cast<volatile LPCG48_SETPOINT*>(0x40CC6608); }
};

// LPCG working status
union LPCG48_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG48_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG48_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG48_STATUS0*>(0x40CC6610); }
};

// LPCG low power status
union LPCG48_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG48_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG48_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG48_STATUS1*>(0x40CC6614); }
};

// LPCG configuration
union LPCG48_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG48_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG48_CONFIG &ref() { return *reinterpret_cast<volatile LPCG48_CONFIG*>(0x40CC6618); }
};

// LPCG access control
union LPCG48_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG48_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG48_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG48_AUTHEN*>(0x40CC661C); }
};

// LPCG direct control
union LPCG49_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG49_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG49_DIRECT &ref() { return *reinterpret_cast<volatile LPCG49_DIRECT*>(0x40CC6620); }
};

// LPCG domain control
union LPCG49_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG49_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG49_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG49_DOMAIN*>(0x40CC6624); }
};

// LPCG working status
union LPCG49_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG49_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG49_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG49_STATUS0*>(0x40CC6630); }
};

// LPCG low power status
union LPCG49_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG49_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG49_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG49_STATUS1*>(0x40CC6634); }
};

// LPCG configuration
union LPCG49_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG49_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG49_CONFIG &ref() { return *reinterpret_cast<volatile LPCG49_CONFIG*>(0x40CC6638); }
};

// LPCG access control
union LPCG49_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG49_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG49_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG49_AUTHEN*>(0x40CC663C); }
};

// LPCG direct control
union LPCG50_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG50_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG50_DIRECT &ref() { return *reinterpret_cast<volatile LPCG50_DIRECT*>(0x40CC6640); }
};

// LPCG domain control
union LPCG50_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG50_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG50_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG50_DOMAIN*>(0x40CC6644); }
};

// LPCG working status
union LPCG50_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG50_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG50_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG50_STATUS0*>(0x40CC6650); }
};

// LPCG low power status
union LPCG50_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG50_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG50_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG50_STATUS1*>(0x40CC6654); }
};

// LPCG configuration
union LPCG50_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG50_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG50_CONFIG &ref() { return *reinterpret_cast<volatile LPCG50_CONFIG*>(0x40CC6658); }
};

// LPCG access control
union LPCG50_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG50_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG50_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG50_AUTHEN*>(0x40CC665C); }
};

// LPCG direct control
union LPCG51_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG51_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG51_DIRECT &ref() { return *reinterpret_cast<volatile LPCG51_DIRECT*>(0x40CC6660); }
};

// LPCG domain control
union LPCG51_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG51_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG51_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG51_DOMAIN*>(0x40CC6664); }
};

// LPCG working status
union LPCG51_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG51_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG51_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG51_STATUS0*>(0x40CC6670); }
};

// LPCG low power status
union LPCG51_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG51_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG51_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG51_STATUS1*>(0x40CC6674); }
};

// LPCG configuration
union LPCG51_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG51_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG51_CONFIG &ref() { return *reinterpret_cast<volatile LPCG51_CONFIG*>(0x40CC6678); }
};

// LPCG access control
union LPCG51_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG51_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG51_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG51_AUTHEN*>(0x40CC667C); }
};

// LPCG direct control
union LPCG52_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG52_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG52_DIRECT &ref() { return *reinterpret_cast<volatile LPCG52_DIRECT*>(0x40CC6680); }
};

// LPCG domain control
union LPCG52_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG52_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG52_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG52_DOMAIN*>(0x40CC6684); }
};

// LPCG working status
union LPCG52_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG52_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG52_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG52_STATUS0*>(0x40CC6690); }
};

// LPCG low power status
union LPCG52_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG52_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG52_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG52_STATUS1*>(0x40CC6694); }
};

// LPCG configuration
union LPCG52_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG52_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG52_CONFIG &ref() { return *reinterpret_cast<volatile LPCG52_CONFIG*>(0x40CC6698); }
};

// LPCG access control
union LPCG52_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG52_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG52_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG52_AUTHEN*>(0x40CC669C); }
};

// LPCG direct control
union LPCG53_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG53_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG53_DIRECT &ref() { return *reinterpret_cast<volatile LPCG53_DIRECT*>(0x40CC66A0); }
};

// LPCG domain control
union LPCG53_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG53_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG53_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG53_DOMAIN*>(0x40CC66A4); }
};

// LPCG working status
union LPCG53_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG53_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG53_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG53_STATUS0*>(0x40CC66B0); }
};

// LPCG low power status
union LPCG53_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG53_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG53_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG53_STATUS1*>(0x40CC66B4); }
};

// LPCG configuration
union LPCG53_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG53_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG53_CONFIG &ref() { return *reinterpret_cast<volatile LPCG53_CONFIG*>(0x40CC66B8); }
};

// LPCG access control
union LPCG53_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG53_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG53_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG53_AUTHEN*>(0x40CC66BC); }
};

// LPCG direct control
union LPCG54_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG54_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG54_DIRECT &ref() { return *reinterpret_cast<volatile LPCG54_DIRECT*>(0x40CC66C0); }
};

// LPCG domain control
union LPCG54_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG54_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG54_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG54_DOMAIN*>(0x40CC66C4); }
};

// LPCG working status
union LPCG54_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG54_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG54_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG54_STATUS0*>(0x40CC66D0); }
};

// LPCG low power status
union LPCG54_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG54_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG54_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG54_STATUS1*>(0x40CC66D4); }
};

// LPCG configuration
union LPCG54_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG54_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG54_CONFIG &ref() { return *reinterpret_cast<volatile LPCG54_CONFIG*>(0x40CC66D8); }
};

// LPCG access control
union LPCG54_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG54_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG54_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG54_AUTHEN*>(0x40CC66DC); }
};

// LPCG direct control
union LPCG55_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG55_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG55_DIRECT &ref() { return *reinterpret_cast<volatile LPCG55_DIRECT*>(0x40CC66E0); }
};

// LPCG domain control
union LPCG55_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG55_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG55_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG55_DOMAIN*>(0x40CC66E4); }
};

// LPCG working status
union LPCG55_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG55_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG55_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG55_STATUS0*>(0x40CC66F0); }
};

// LPCG low power status
union LPCG55_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG55_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG55_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG55_STATUS1*>(0x40CC66F4); }
};

// LPCG configuration
union LPCG55_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG55_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG55_CONFIG &ref() { return *reinterpret_cast<volatile LPCG55_CONFIG*>(0x40CC66F8); }
};

// LPCG access control
union LPCG55_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG55_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG55_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG55_AUTHEN*>(0x40CC66FC); }
};

// LPCG direct control
union LPCG56_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG56_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG56_DIRECT &ref() { return *reinterpret_cast<volatile LPCG56_DIRECT*>(0x40CC6700); }
};

// LPCG domain control
union LPCG56_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG56_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG56_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG56_DOMAIN*>(0x40CC6704); }
};

// LPCG working status
union LPCG56_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG56_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG56_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG56_STATUS0*>(0x40CC6710); }
};

// LPCG low power status
union LPCG56_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG56_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG56_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG56_STATUS1*>(0x40CC6714); }
};

// LPCG configuration
union LPCG56_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG56_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG56_CONFIG &ref() { return *reinterpret_cast<volatile LPCG56_CONFIG*>(0x40CC6718); }
};

// LPCG access control
union LPCG56_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG56_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG56_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG56_AUTHEN*>(0x40CC671C); }
};

// LPCG direct control
union LPCG57_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG57_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG57_DIRECT &ref() { return *reinterpret_cast<volatile LPCG57_DIRECT*>(0x40CC6720); }
};

// LPCG domain control
union LPCG57_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG57_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG57_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG57_DOMAIN*>(0x40CC6724); }
};

// LPCG working status
union LPCG57_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG57_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG57_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG57_STATUS0*>(0x40CC6730); }
};

// LPCG low power status
union LPCG57_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG57_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG57_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG57_STATUS1*>(0x40CC6734); }
};

// LPCG configuration
union LPCG57_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG57_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG57_CONFIG &ref() { return *reinterpret_cast<volatile LPCG57_CONFIG*>(0x40CC6738); }
};

// LPCG access control
union LPCG57_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG57_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG57_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG57_AUTHEN*>(0x40CC673C); }
};

// LPCG direct control
union LPCG58_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG58_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG58_DIRECT &ref() { return *reinterpret_cast<volatile LPCG58_DIRECT*>(0x40CC6740); }
};

// LPCG domain control
union LPCG58_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG58_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG58_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG58_DOMAIN*>(0x40CC6744); }
};

// LPCG working status
union LPCG58_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG58_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG58_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG58_STATUS0*>(0x40CC6750); }
};

// LPCG low power status
union LPCG58_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG58_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG58_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG58_STATUS1*>(0x40CC6754); }
};

// LPCG configuration
union LPCG58_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG58_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG58_CONFIG &ref() { return *reinterpret_cast<volatile LPCG58_CONFIG*>(0x40CC6758); }
};

// LPCG access control
union LPCG58_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG58_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG58_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG58_AUTHEN*>(0x40CC675C); }
};

// LPCG direct control
union LPCG59_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG59_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG59_DIRECT &ref() { return *reinterpret_cast<volatile LPCG59_DIRECT*>(0x40CC6760); }
};

// LPCG domain control
union LPCG59_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG59_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG59_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG59_DOMAIN*>(0x40CC6764); }
};

// LPCG working status
union LPCG59_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG59_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG59_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG59_STATUS0*>(0x40CC6770); }
};

// LPCG low power status
union LPCG59_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG59_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG59_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG59_STATUS1*>(0x40CC6774); }
};

// LPCG configuration
union LPCG59_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG59_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG59_CONFIG &ref() { return *reinterpret_cast<volatile LPCG59_CONFIG*>(0x40CC6778); }
};

// LPCG access control
union LPCG59_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG59_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG59_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG59_AUTHEN*>(0x40CC677C); }
};

// LPCG direct control
union LPCG60_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG60_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG60_DIRECT &ref() { return *reinterpret_cast<volatile LPCG60_DIRECT*>(0x40CC6780); }
};

// LPCG domain control
union LPCG60_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG60_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG60_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG60_DOMAIN*>(0x40CC6784); }
};

// LPCG working status
union LPCG60_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG60_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG60_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG60_STATUS0*>(0x40CC6790); }
};

// LPCG low power status
union LPCG60_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG60_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG60_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG60_STATUS1*>(0x40CC6794); }
};

// LPCG configuration
union LPCG60_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG60_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG60_CONFIG &ref() { return *reinterpret_cast<volatile LPCG60_CONFIG*>(0x40CC6798); }
};

// LPCG access control
union LPCG60_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG60_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG60_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG60_AUTHEN*>(0x40CC679C); }
};

// LPCG direct control
union LPCG61_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG61_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG61_DIRECT &ref() { return *reinterpret_cast<volatile LPCG61_DIRECT*>(0x40CC67A0); }
};

// LPCG domain control
union LPCG61_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG61_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG61_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG61_DOMAIN*>(0x40CC67A4); }
};

// LPCG working status
union LPCG61_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG61_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG61_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG61_STATUS0*>(0x40CC67B0); }
};

// LPCG low power status
union LPCG61_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG61_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG61_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG61_STATUS1*>(0x40CC67B4); }
};

// LPCG configuration
union LPCG61_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG61_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG61_CONFIG &ref() { return *reinterpret_cast<volatile LPCG61_CONFIG*>(0x40CC67B8); }
};

// LPCG access control
union LPCG61_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG61_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG61_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG61_AUTHEN*>(0x40CC67BC); }
};

// LPCG direct control
union LPCG62_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG62_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG62_DIRECT &ref() { return *reinterpret_cast<volatile LPCG62_DIRECT*>(0x40CC67C0); }
};

// LPCG domain control
union LPCG62_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG62_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG62_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG62_DOMAIN*>(0x40CC67C4); }
};

// LPCG working status
union LPCG62_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG62_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG62_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG62_STATUS0*>(0x40CC67D0); }
};

// LPCG low power status
union LPCG62_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG62_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG62_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG62_STATUS1*>(0x40CC67D4); }
};

// LPCG configuration
union LPCG62_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG62_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG62_CONFIG &ref() { return *reinterpret_cast<volatile LPCG62_CONFIG*>(0x40CC67D8); }
};

// LPCG access control
union LPCG62_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG62_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG62_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG62_AUTHEN*>(0x40CC67DC); }
};

// LPCG direct control
union LPCG63_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG63_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG63_DIRECT &ref() { return *reinterpret_cast<volatile LPCG63_DIRECT*>(0x40CC67E0); }
};

// LPCG domain control
union LPCG63_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG63_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG63_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG63_DOMAIN*>(0x40CC67E4); }
};

// LPCG working status
union LPCG63_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG63_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG63_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG63_STATUS0*>(0x40CC67F0); }
};

// LPCG low power status
union LPCG63_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG63_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG63_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG63_STATUS1*>(0x40CC67F4); }
};

// LPCG configuration
union LPCG63_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG63_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG63_CONFIG &ref() { return *reinterpret_cast<volatile LPCG63_CONFIG*>(0x40CC67F8); }
};

// LPCG access control
union LPCG63_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG63_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG63_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG63_AUTHEN*>(0x40CC67FC); }
};

// LPCG direct control
union LPCG64_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG64_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG64_DIRECT &ref() { return *reinterpret_cast<volatile LPCG64_DIRECT*>(0x40CC6800); }
};

// LPCG domain control
union LPCG64_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG64_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG64_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG64_DOMAIN*>(0x40CC6804); }
};

// LPCG working status
union LPCG64_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG64_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG64_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG64_STATUS0*>(0x40CC6810); }
};

// LPCG low power status
union LPCG64_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG64_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG64_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG64_STATUS1*>(0x40CC6814); }
};

// LPCG configuration
union LPCG64_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG64_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG64_CONFIG &ref() { return *reinterpret_cast<volatile LPCG64_CONFIG*>(0x40CC6818); }
};

// LPCG access control
union LPCG64_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG64_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG64_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG64_AUTHEN*>(0x40CC681C); }
};

// LPCG direct control
union LPCG65_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG65_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG65_DIRECT &ref() { return *reinterpret_cast<volatile LPCG65_DIRECT*>(0x40CC6820); }
};

// LPCG domain control
union LPCG65_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG65_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG65_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG65_DOMAIN*>(0x40CC6824); }
};

// LPCG working status
union LPCG65_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG65_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG65_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG65_STATUS0*>(0x40CC6830); }
};

// LPCG low power status
union LPCG65_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG65_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG65_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG65_STATUS1*>(0x40CC6834); }
};

// LPCG configuration
union LPCG65_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG65_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG65_CONFIG &ref() { return *reinterpret_cast<volatile LPCG65_CONFIG*>(0x40CC6838); }
};

// LPCG access control
union LPCG65_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG65_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG65_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG65_AUTHEN*>(0x40CC683C); }
};

// LPCG direct control
union LPCG66_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG66_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG66_DIRECT &ref() { return *reinterpret_cast<volatile LPCG66_DIRECT*>(0x40CC6840); }
};

// LPCG domain control
union LPCG66_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG66_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG66_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG66_DOMAIN*>(0x40CC6844); }
};

// LPCG working status
union LPCG66_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG66_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG66_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG66_STATUS0*>(0x40CC6850); }
};

// LPCG low power status
union LPCG66_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG66_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG66_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG66_STATUS1*>(0x40CC6854); }
};

// LPCG configuration
union LPCG66_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG66_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG66_CONFIG &ref() { return *reinterpret_cast<volatile LPCG66_CONFIG*>(0x40CC6858); }
};

// LPCG access control
union LPCG66_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG66_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG66_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG66_AUTHEN*>(0x40CC685C); }
};

// LPCG direct control
union LPCG67_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG67_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG67_DIRECT &ref() { return *reinterpret_cast<volatile LPCG67_DIRECT*>(0x40CC6860); }
};

// LPCG domain control
union LPCG67_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG67_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG67_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG67_DOMAIN*>(0x40CC6864); }
};

// LPCG working status
union LPCG67_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG67_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG67_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG67_STATUS0*>(0x40CC6870); }
};

// LPCG low power status
union LPCG67_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG67_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG67_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG67_STATUS1*>(0x40CC6874); }
};

// LPCG configuration
union LPCG67_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG67_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG67_CONFIG &ref() { return *reinterpret_cast<volatile LPCG67_CONFIG*>(0x40CC6878); }
};

// LPCG access control
union LPCG67_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG67_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG67_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG67_AUTHEN*>(0x40CC687C); }
};

// LPCG direct control
union LPCG68_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG68_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG68_DIRECT &ref() { return *reinterpret_cast<volatile LPCG68_DIRECT*>(0x40CC6880); }
};

// LPCG domain control
union LPCG68_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG68_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG68_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG68_DOMAIN*>(0x40CC6884); }
};

// LPCG working status
union LPCG68_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG68_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG68_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG68_STATUS0*>(0x40CC6890); }
};

// LPCG low power status
union LPCG68_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG68_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG68_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG68_STATUS1*>(0x40CC6894); }
};

// LPCG configuration
union LPCG68_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG68_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG68_CONFIG &ref() { return *reinterpret_cast<volatile LPCG68_CONFIG*>(0x40CC6898); }
};

// LPCG access control
union LPCG68_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG68_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG68_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG68_AUTHEN*>(0x40CC689C); }
};

// LPCG direct control
union LPCG69_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG69_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG69_DIRECT &ref() { return *reinterpret_cast<volatile LPCG69_DIRECT*>(0x40CC68A0); }
};

// LPCG domain control
union LPCG69_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG69_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG69_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG69_DOMAIN*>(0x40CC68A4); }
};

// LPCG working status
union LPCG69_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG69_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG69_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG69_STATUS0*>(0x40CC68B0); }
};

// LPCG low power status
union LPCG69_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG69_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG69_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG69_STATUS1*>(0x40CC68B4); }
};

// LPCG configuration
union LPCG69_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG69_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG69_CONFIG &ref() { return *reinterpret_cast<volatile LPCG69_CONFIG*>(0x40CC68B8); }
};

// LPCG access control
union LPCG69_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG69_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG69_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG69_AUTHEN*>(0x40CC68BC); }
};

// LPCG direct control
union LPCG70_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG70_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG70_DIRECT &ref() { return *reinterpret_cast<volatile LPCG70_DIRECT*>(0x40CC68C0); }
};

// LPCG domain control
union LPCG70_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG70_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG70_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG70_DOMAIN*>(0x40CC68C4); }
};

// LPCG working status
union LPCG70_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG70_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG70_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG70_STATUS0*>(0x40CC68D0); }
};

// LPCG low power status
union LPCG70_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG70_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG70_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG70_STATUS1*>(0x40CC68D4); }
};

// LPCG configuration
union LPCG70_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG70_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG70_CONFIG &ref() { return *reinterpret_cast<volatile LPCG70_CONFIG*>(0x40CC68D8); }
};

// LPCG access control
union LPCG70_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG70_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG70_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG70_AUTHEN*>(0x40CC68DC); }
};

// LPCG direct control
union LPCG71_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG71_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG71_DIRECT &ref() { return *reinterpret_cast<volatile LPCG71_DIRECT*>(0x40CC68E0); }
};

// LPCG domain control
union LPCG71_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG71_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG71_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG71_DOMAIN*>(0x40CC68E4); }
};

// LPCG working status
union LPCG71_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG71_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG71_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG71_STATUS0*>(0x40CC68F0); }
};

// LPCG low power status
union LPCG71_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG71_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG71_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG71_STATUS1*>(0x40CC68F4); }
};

// LPCG configuration
union LPCG71_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG71_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG71_CONFIG &ref() { return *reinterpret_cast<volatile LPCG71_CONFIG*>(0x40CC68F8); }
};

// LPCG access control
union LPCG71_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG71_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG71_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG71_AUTHEN*>(0x40CC68FC); }
};

// LPCG direct control
union LPCG72_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG72_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG72_DIRECT &ref() { return *reinterpret_cast<volatile LPCG72_DIRECT*>(0x40CC6900); }
};

// LPCG domain control
union LPCG72_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG72_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG72_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG72_DOMAIN*>(0x40CC6904); }
};

// LPCG working status
union LPCG72_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG72_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG72_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG72_STATUS0*>(0x40CC6910); }
};

// LPCG low power status
union LPCG72_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG72_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG72_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG72_STATUS1*>(0x40CC6914); }
};

// LPCG configuration
union LPCG72_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG72_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG72_CONFIG &ref() { return *reinterpret_cast<volatile LPCG72_CONFIG*>(0x40CC6918); }
};

// LPCG access control
union LPCG72_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG72_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG72_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG72_AUTHEN*>(0x40CC691C); }
};

// LPCG direct control
union LPCG73_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG73_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG73_DIRECT &ref() { return *reinterpret_cast<volatile LPCG73_DIRECT*>(0x40CC6920); }
};

// LPCG domain control
union LPCG73_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG73_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG73_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG73_DOMAIN*>(0x40CC6924); }
};

// LPCG working status
union LPCG73_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG73_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG73_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG73_STATUS0*>(0x40CC6930); }
};

// LPCG low power status
union LPCG73_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG73_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG73_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG73_STATUS1*>(0x40CC6934); }
};

// LPCG configuration
union LPCG73_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG73_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG73_CONFIG &ref() { return *reinterpret_cast<volatile LPCG73_CONFIG*>(0x40CC6938); }
};

// LPCG access control
union LPCG73_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG73_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG73_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG73_AUTHEN*>(0x40CC693C); }
};

// LPCG direct control
union LPCG74_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG74_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG74_DIRECT &ref() { return *reinterpret_cast<volatile LPCG74_DIRECT*>(0x40CC6940); }
};

// LPCG domain control
union LPCG74_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG74_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG74_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG74_DOMAIN*>(0x40CC6944); }
};

// LPCG working status
union LPCG74_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG74_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG74_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG74_STATUS0*>(0x40CC6950); }
};

// LPCG low power status
union LPCG74_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG74_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG74_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG74_STATUS1*>(0x40CC6954); }
};

// LPCG configuration
union LPCG74_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG74_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG74_CONFIG &ref() { return *reinterpret_cast<volatile LPCG74_CONFIG*>(0x40CC6958); }
};

// LPCG access control
union LPCG74_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG74_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG74_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG74_AUTHEN*>(0x40CC695C); }
};

// LPCG direct control
union LPCG75_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG75_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG75_DIRECT &ref() { return *reinterpret_cast<volatile LPCG75_DIRECT*>(0x40CC6960); }
};

// LPCG domain control
union LPCG75_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG75_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG75_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG75_DOMAIN*>(0x40CC6964); }
};

// LPCG working status
union LPCG75_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG75_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG75_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG75_STATUS0*>(0x40CC6970); }
};

// LPCG low power status
union LPCG75_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG75_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG75_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG75_STATUS1*>(0x40CC6974); }
};

// LPCG configuration
union LPCG75_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG75_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG75_CONFIG &ref() { return *reinterpret_cast<volatile LPCG75_CONFIG*>(0x40CC6978); }
};

// LPCG access control
union LPCG75_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG75_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG75_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG75_AUTHEN*>(0x40CC697C); }
};

// LPCG direct control
union LPCG76_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG76_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG76_DIRECT &ref() { return *reinterpret_cast<volatile LPCG76_DIRECT*>(0x40CC6980); }
};

// LPCG domain control
union LPCG76_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG76_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG76_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG76_DOMAIN*>(0x40CC6984); }
};

// LPCG working status
union LPCG76_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG76_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG76_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG76_STATUS0*>(0x40CC6990); }
};

// LPCG low power status
union LPCG76_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG76_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG76_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG76_STATUS1*>(0x40CC6994); }
};

// LPCG configuration
union LPCG76_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG76_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG76_CONFIG &ref() { return *reinterpret_cast<volatile LPCG76_CONFIG*>(0x40CC6998); }
};

// LPCG access control
union LPCG76_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG76_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG76_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG76_AUTHEN*>(0x40CC699C); }
};

// LPCG direct control
union LPCG77_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG77_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG77_DIRECT &ref() { return *reinterpret_cast<volatile LPCG77_DIRECT*>(0x40CC69A0); }
};

// LPCG domain control
union LPCG77_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG77_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG77_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG77_DOMAIN*>(0x40CC69A4); }
};

// LPCG working status
union LPCG77_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG77_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG77_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG77_STATUS0*>(0x40CC69B0); }
};

// LPCG low power status
union LPCG77_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG77_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG77_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG77_STATUS1*>(0x40CC69B4); }
};

// LPCG configuration
union LPCG77_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG77_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG77_CONFIG &ref() { return *reinterpret_cast<volatile LPCG77_CONFIG*>(0x40CC69B8); }
};

// LPCG access control
union LPCG77_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG77_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG77_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG77_AUTHEN*>(0x40CC69BC); }
};

// LPCG direct control
union LPCG78_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG78_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG78_DIRECT &ref() { return *reinterpret_cast<volatile LPCG78_DIRECT*>(0x40CC69C0); }
};

// LPCG domain control
union LPCG78_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG78_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG78_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG78_DOMAIN*>(0x40CC69C4); }
};

// LPCG working status
union LPCG78_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG78_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG78_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG78_STATUS0*>(0x40CC69D0); }
};

// LPCG low power status
union LPCG78_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG78_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG78_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG78_STATUS1*>(0x40CC69D4); }
};

// LPCG configuration
union LPCG78_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG78_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG78_CONFIG &ref() { return *reinterpret_cast<volatile LPCG78_CONFIG*>(0x40CC69D8); }
};

// LPCG access control
union LPCG78_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG78_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG78_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG78_AUTHEN*>(0x40CC69DC); }
};

// LPCG direct control
union LPCG79_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG79_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG79_DIRECT &ref() { return *reinterpret_cast<volatile LPCG79_DIRECT*>(0x40CC69E0); }
};

// LPCG domain control
union LPCG79_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG79_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG79_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG79_DOMAIN*>(0x40CC69E4); }
};

// LPCG working status
union LPCG79_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG79_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG79_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG79_STATUS0*>(0x40CC69F0); }
};

// LPCG low power status
union LPCG79_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG79_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG79_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG79_STATUS1*>(0x40CC69F4); }
};

// LPCG configuration
union LPCG79_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG79_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG79_CONFIG &ref() { return *reinterpret_cast<volatile LPCG79_CONFIG*>(0x40CC69F8); }
};

// LPCG access control
union LPCG79_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG79_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG79_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG79_AUTHEN*>(0x40CC69FC); }
};

// LPCG direct control
union LPCG80_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG80_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG80_DIRECT &ref() { return *reinterpret_cast<volatile LPCG80_DIRECT*>(0x40CC6A00); }
};

// LPCG domain control
union LPCG80_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG80_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG80_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG80_DOMAIN*>(0x40CC6A04); }
};

// LPCG working status
union LPCG80_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG80_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG80_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG80_STATUS0*>(0x40CC6A10); }
};

// LPCG low power status
union LPCG80_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG80_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG80_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG80_STATUS1*>(0x40CC6A14); }
};

// LPCG configuration
union LPCG80_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG80_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG80_CONFIG &ref() { return *reinterpret_cast<volatile LPCG80_CONFIG*>(0x40CC6A18); }
};

// LPCG access control
union LPCG80_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG80_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG80_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG80_AUTHEN*>(0x40CC6A1C); }
};

// LPCG direct control
union LPCG81_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG81_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG81_DIRECT &ref() { return *reinterpret_cast<volatile LPCG81_DIRECT*>(0x40CC6A20); }
};

// LPCG domain control
union LPCG81_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG81_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG81_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG81_DOMAIN*>(0x40CC6A24); }
};

// LPCG working status
union LPCG81_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG81_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG81_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG81_STATUS0*>(0x40CC6A30); }
};

// LPCG low power status
union LPCG81_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG81_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG81_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG81_STATUS1*>(0x40CC6A34); }
};

// LPCG configuration
union LPCG81_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG81_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG81_CONFIG &ref() { return *reinterpret_cast<volatile LPCG81_CONFIG*>(0x40CC6A38); }
};

// LPCG access control
union LPCG81_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG81_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG81_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG81_AUTHEN*>(0x40CC6A3C); }
};

// LPCG direct control
union LPCG82_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG82_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG82_DIRECT &ref() { return *reinterpret_cast<volatile LPCG82_DIRECT*>(0x40CC6A40); }
};

// LPCG domain control
union LPCG82_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG82_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG82_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG82_DOMAIN*>(0x40CC6A44); }
};

// LPCG working status
union LPCG82_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG82_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG82_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG82_STATUS0*>(0x40CC6A50); }
};

// LPCG low power status
union LPCG82_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG82_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG82_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG82_STATUS1*>(0x40CC6A54); }
};

// LPCG configuration
union LPCG82_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG82_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG82_CONFIG &ref() { return *reinterpret_cast<volatile LPCG82_CONFIG*>(0x40CC6A58); }
};

// LPCG access control
union LPCG82_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG82_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG82_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG82_AUTHEN*>(0x40CC6A5C); }
};

// LPCG direct control
union LPCG83_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG83_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG83_DIRECT &ref() { return *reinterpret_cast<volatile LPCG83_DIRECT*>(0x40CC6A60); }
};

// LPCG domain control
union LPCG83_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG83_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG83_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG83_DOMAIN*>(0x40CC6A64); }
};

// LPCG working status
union LPCG83_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG83_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG83_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG83_STATUS0*>(0x40CC6A70); }
};

// LPCG low power status
union LPCG83_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG83_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG83_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG83_STATUS1*>(0x40CC6A74); }
};

// LPCG configuration
union LPCG83_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG83_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG83_CONFIG &ref() { return *reinterpret_cast<volatile LPCG83_CONFIG*>(0x40CC6A78); }
};

// LPCG access control
union LPCG83_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG83_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG83_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG83_AUTHEN*>(0x40CC6A7C); }
};

// LPCG direct control
union LPCG84_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG84_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG84_DIRECT &ref() { return *reinterpret_cast<volatile LPCG84_DIRECT*>(0x40CC6A80); }
};

// LPCG domain control
union LPCG84_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG84_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG84_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG84_DOMAIN*>(0x40CC6A84); }
};

// LPCG working status
union LPCG84_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG84_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG84_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG84_STATUS0*>(0x40CC6A90); }
};

// LPCG low power status
union LPCG84_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG84_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG84_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG84_STATUS1*>(0x40CC6A94); }
};

// LPCG configuration
union LPCG84_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG84_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG84_CONFIG &ref() { return *reinterpret_cast<volatile LPCG84_CONFIG*>(0x40CC6A98); }
};

// LPCG access control
union LPCG84_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG84_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG84_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG84_AUTHEN*>(0x40CC6A9C); }
};

// LPCG direct control
union LPCG85_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG85_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG85_DIRECT &ref() { return *reinterpret_cast<volatile LPCG85_DIRECT*>(0x40CC6AA0); }
};

// LPCG domain control
union LPCG85_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG85_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG85_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG85_DOMAIN*>(0x40CC6AA4); }
};

// LPCG working status
union LPCG85_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG85_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG85_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG85_STATUS0*>(0x40CC6AB0); }
};

// LPCG low power status
union LPCG85_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG85_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG85_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG85_STATUS1*>(0x40CC6AB4); }
};

// LPCG configuration
union LPCG85_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG85_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG85_CONFIG &ref() { return *reinterpret_cast<volatile LPCG85_CONFIG*>(0x40CC6AB8); }
};

// LPCG access control
union LPCG85_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG85_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG85_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG85_AUTHEN*>(0x40CC6ABC); }
};

// LPCG direct control
union LPCG86_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG86_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG86_DIRECT &ref() { return *reinterpret_cast<volatile LPCG86_DIRECT*>(0x40CC6AC0); }
};

// LPCG domain control
union LPCG86_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG86_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG86_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG86_DOMAIN*>(0x40CC6AC4); }
};

// LPCG working status
union LPCG86_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG86_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG86_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG86_STATUS0*>(0x40CC6AD0); }
};

// LPCG low power status
union LPCG86_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG86_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG86_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG86_STATUS1*>(0x40CC6AD4); }
};

// LPCG configuration
union LPCG86_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG86_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG86_CONFIG &ref() { return *reinterpret_cast<volatile LPCG86_CONFIG*>(0x40CC6AD8); }
};

// LPCG access control
union LPCG86_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG86_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG86_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG86_AUTHEN*>(0x40CC6ADC); }
};

// LPCG direct control
union LPCG87_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG87_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG87_DIRECT &ref() { return *reinterpret_cast<volatile LPCG87_DIRECT*>(0x40CC6AE0); }
};

// LPCG domain control
union LPCG87_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG87_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG87_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG87_DOMAIN*>(0x40CC6AE4); }
};

// LPCG working status
union LPCG87_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG87_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG87_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG87_STATUS0*>(0x40CC6AF0); }
};

// LPCG low power status
union LPCG87_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG87_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG87_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG87_STATUS1*>(0x40CC6AF4); }
};

// LPCG configuration
union LPCG87_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG87_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG87_CONFIG &ref() { return *reinterpret_cast<volatile LPCG87_CONFIG*>(0x40CC6AF8); }
};

// LPCG access control
union LPCG87_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG87_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG87_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG87_AUTHEN*>(0x40CC6AFC); }
};

// LPCG direct control
union LPCG88_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG88_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG88_DIRECT &ref() { return *reinterpret_cast<volatile LPCG88_DIRECT*>(0x40CC6B00); }
};

// LPCG domain control
union LPCG88_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG88_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG88_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG88_DOMAIN*>(0x40CC6B04); }
};

// LPCG working status
union LPCG88_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG88_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG88_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG88_STATUS0*>(0x40CC6B10); }
};

// LPCG low power status
union LPCG88_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG88_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG88_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG88_STATUS1*>(0x40CC6B14); }
};

// LPCG configuration
union LPCG88_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG88_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG88_CONFIG &ref() { return *reinterpret_cast<volatile LPCG88_CONFIG*>(0x40CC6B18); }
};

// LPCG access control
union LPCG88_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG88_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG88_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG88_AUTHEN*>(0x40CC6B1C); }
};

// LPCG direct control
union LPCG89_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG89_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG89_DIRECT &ref() { return *reinterpret_cast<volatile LPCG89_DIRECT*>(0x40CC6B20); }
};

// LPCG domain control
union LPCG89_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG89_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG89_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG89_DOMAIN*>(0x40CC6B24); }
};

// LPCG working status
union LPCG89_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG89_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG89_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG89_STATUS0*>(0x40CC6B30); }
};

// LPCG low power status
union LPCG89_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG89_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG89_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG89_STATUS1*>(0x40CC6B34); }
};

// LPCG configuration
union LPCG89_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG89_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG89_CONFIG &ref() { return *reinterpret_cast<volatile LPCG89_CONFIG*>(0x40CC6B38); }
};

// LPCG access control
union LPCG89_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG89_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG89_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG89_AUTHEN*>(0x40CC6B3C); }
};

// LPCG direct control
union LPCG90_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG90_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG90_DIRECT &ref() { return *reinterpret_cast<volatile LPCG90_DIRECT*>(0x40CC6B40); }
};

// LPCG domain control
union LPCG90_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG90_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG90_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG90_DOMAIN*>(0x40CC6B44); }
};

// LPCG working status
union LPCG90_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG90_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG90_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG90_STATUS0*>(0x40CC6B50); }
};

// LPCG low power status
union LPCG90_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG90_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG90_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG90_STATUS1*>(0x40CC6B54); }
};

// LPCG configuration
union LPCG90_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG90_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG90_CONFIG &ref() { return *reinterpret_cast<volatile LPCG90_CONFIG*>(0x40CC6B58); }
};

// LPCG access control
union LPCG90_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG90_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG90_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG90_AUTHEN*>(0x40CC6B5C); }
};

// LPCG direct control
union LPCG91_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG91_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG91_DIRECT &ref() { return *reinterpret_cast<volatile LPCG91_DIRECT*>(0x40CC6B60); }
};

// LPCG domain control
union LPCG91_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG91_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG91_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG91_DOMAIN*>(0x40CC6B64); }
};

// LPCG working status
union LPCG91_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG91_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG91_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG91_STATUS0*>(0x40CC6B70); }
};

// LPCG low power status
union LPCG91_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG91_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG91_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG91_STATUS1*>(0x40CC6B74); }
};

// LPCG configuration
union LPCG91_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG91_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG91_CONFIG &ref() { return *reinterpret_cast<volatile LPCG91_CONFIG*>(0x40CC6B78); }
};

// LPCG access control
union LPCG91_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG91_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG91_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG91_AUTHEN*>(0x40CC6B7C); }
};

// LPCG direct control
union LPCG92_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG92_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG92_DIRECT &ref() { return *reinterpret_cast<volatile LPCG92_DIRECT*>(0x40CC6B80); }
};

// LPCG domain control
union LPCG92_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG92_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG92_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG92_DOMAIN*>(0x40CC6B84); }
};

// LPCG working status
union LPCG92_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG92_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG92_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG92_STATUS0*>(0x40CC6B90); }
};

// LPCG low power status
union LPCG92_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG92_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG92_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG92_STATUS1*>(0x40CC6B94); }
};

// LPCG configuration
union LPCG92_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG92_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG92_CONFIG &ref() { return *reinterpret_cast<volatile LPCG92_CONFIG*>(0x40CC6B98); }
};

// LPCG access control
union LPCG92_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG92_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG92_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG92_AUTHEN*>(0x40CC6B9C); }
};

// LPCG direct control
union LPCG93_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG93_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG93_DIRECT &ref() { return *reinterpret_cast<volatile LPCG93_DIRECT*>(0x40CC6BA0); }
};

// LPCG domain control
union LPCG93_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG93_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG93_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG93_DOMAIN*>(0x40CC6BA4); }
};

// LPCG working status
union LPCG93_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG93_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG93_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG93_STATUS0*>(0x40CC6BB0); }
};

// LPCG low power status
union LPCG93_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG93_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG93_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG93_STATUS1*>(0x40CC6BB4); }
};

// LPCG configuration
union LPCG93_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG93_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG93_CONFIG &ref() { return *reinterpret_cast<volatile LPCG93_CONFIG*>(0x40CC6BB8); }
};

// LPCG access control
union LPCG93_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG93_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG93_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG93_AUTHEN*>(0x40CC6BBC); }
};

// LPCG direct control
union LPCG94_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG94_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG94_DIRECT &ref() { return *reinterpret_cast<volatile LPCG94_DIRECT*>(0x40CC6BC0); }
};

// LPCG domain control
union LPCG94_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG94_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG94_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG94_DOMAIN*>(0x40CC6BC4); }
};

// LPCG working status
union LPCG94_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG94_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG94_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG94_STATUS0*>(0x40CC6BD0); }
};

// LPCG low power status
union LPCG94_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG94_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG94_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG94_STATUS1*>(0x40CC6BD4); }
};

// LPCG configuration
union LPCG94_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG94_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG94_CONFIG &ref() { return *reinterpret_cast<volatile LPCG94_CONFIG*>(0x40CC6BD8); }
};

// LPCG access control
union LPCG94_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG94_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG94_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG94_AUTHEN*>(0x40CC6BDC); }
};

// LPCG direct control
union LPCG95_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG95_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG95_DIRECT &ref() { return *reinterpret_cast<volatile LPCG95_DIRECT*>(0x40CC6BE0); }
};

// LPCG domain control
union LPCG95_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG95_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG95_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG95_DOMAIN*>(0x40CC6BE4); }
};

// LPCG working status
union LPCG95_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG95_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG95_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG95_STATUS0*>(0x40CC6BF0); }
};

// LPCG low power status
union LPCG95_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG95_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG95_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG95_STATUS1*>(0x40CC6BF4); }
};

// LPCG configuration
union LPCG95_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG95_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG95_CONFIG &ref() { return *reinterpret_cast<volatile LPCG95_CONFIG*>(0x40CC6BF8); }
};

// LPCG access control
union LPCG95_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG95_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG95_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG95_AUTHEN*>(0x40CC6BFC); }
};

// LPCG direct control
union LPCG96_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG96_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG96_DIRECT &ref() { return *reinterpret_cast<volatile LPCG96_DIRECT*>(0x40CC6C00); }
};

// LPCG domain control
union LPCG96_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG96_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG96_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG96_DOMAIN*>(0x40CC6C04); }
};

// LPCG working status
union LPCG96_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG96_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG96_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG96_STATUS0*>(0x40CC6C10); }
};

// LPCG low power status
union LPCG96_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG96_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG96_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG96_STATUS1*>(0x40CC6C14); }
};

// LPCG configuration
union LPCG96_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG96_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG96_CONFIG &ref() { return *reinterpret_cast<volatile LPCG96_CONFIG*>(0x40CC6C18); }
};

// LPCG access control
union LPCG96_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG96_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG96_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG96_AUTHEN*>(0x40CC6C1C); }
};

// LPCG direct control
union LPCG97_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG97_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG97_DIRECT &ref() { return *reinterpret_cast<volatile LPCG97_DIRECT*>(0x40CC6C20); }
};

// LPCG domain control
union LPCG97_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG97_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG97_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG97_DOMAIN*>(0x40CC6C24); }
};

// LPCG working status
union LPCG97_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG97_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG97_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG97_STATUS0*>(0x40CC6C30); }
};

// LPCG low power status
union LPCG97_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG97_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG97_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG97_STATUS1*>(0x40CC6C34); }
};

// LPCG configuration
union LPCG97_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG97_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG97_CONFIG &ref() { return *reinterpret_cast<volatile LPCG97_CONFIG*>(0x40CC6C38); }
};

// LPCG access control
union LPCG97_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG97_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG97_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG97_AUTHEN*>(0x40CC6C3C); }
};

// LPCG direct control
union LPCG98_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG98_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG98_DIRECT &ref() { return *reinterpret_cast<volatile LPCG98_DIRECT*>(0x40CC6C40); }
};

// LPCG domain control
union LPCG98_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG98_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG98_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG98_DOMAIN*>(0x40CC6C44); }
};

// LPCG working status
union LPCG98_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG98_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG98_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG98_STATUS0*>(0x40CC6C50); }
};

// LPCG low power status
union LPCG98_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG98_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG98_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG98_STATUS1*>(0x40CC6C54); }
};

// LPCG configuration
union LPCG98_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG98_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG98_CONFIG &ref() { return *reinterpret_cast<volatile LPCG98_CONFIG*>(0x40CC6C58); }
};

// LPCG access control
union LPCG98_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG98_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG98_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG98_AUTHEN*>(0x40CC6C5C); }
};

// LPCG direct control
union LPCG99_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG99_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG99_DIRECT &ref() { return *reinterpret_cast<volatile LPCG99_DIRECT*>(0x40CC6C60); }
};

// LPCG domain control
union LPCG99_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG99_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG99_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG99_DOMAIN*>(0x40CC6C64); }
};

// LPCG working status
union LPCG99_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG99_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG99_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG99_STATUS0*>(0x40CC6C70); }
};

// LPCG low power status
union LPCG99_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG99_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG99_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG99_STATUS1*>(0x40CC6C74); }
};

// LPCG configuration
union LPCG99_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG99_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG99_CONFIG &ref() { return *reinterpret_cast<volatile LPCG99_CONFIG*>(0x40CC6C78); }
};

// LPCG access control
union LPCG99_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG99_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG99_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG99_AUTHEN*>(0x40CC6C7C); }
};

// LPCG direct control
union LPCG100_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG100_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG100_DIRECT &ref() { return *reinterpret_cast<volatile LPCG100_DIRECT*>(0x40CC6C80); }
};

// LPCG domain control
union LPCG100_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG100_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG100_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG100_DOMAIN*>(0x40CC6C84); }
};

// LPCG working status
union LPCG100_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG100_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG100_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG100_STATUS0*>(0x40CC6C90); }
};

// LPCG low power status
union LPCG100_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG100_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG100_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG100_STATUS1*>(0x40CC6C94); }
};

// LPCG configuration
union LPCG100_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG100_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG100_CONFIG &ref() { return *reinterpret_cast<volatile LPCG100_CONFIG*>(0x40CC6C98); }
};

// LPCG access control
union LPCG100_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG100_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG100_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG100_AUTHEN*>(0x40CC6C9C); }
};

// LPCG direct control
union LPCG101_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG101_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG101_DIRECT &ref() { return *reinterpret_cast<volatile LPCG101_DIRECT*>(0x40CC6CA0); }
};

// LPCG domain control
union LPCG101_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG101_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG101_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG101_DOMAIN*>(0x40CC6CA4); }
};

// LPCG working status
union LPCG101_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG101_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG101_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG101_STATUS0*>(0x40CC6CB0); }
};

// LPCG low power status
union LPCG101_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG101_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG101_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG101_STATUS1*>(0x40CC6CB4); }
};

// LPCG configuration
union LPCG101_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG101_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG101_CONFIG &ref() { return *reinterpret_cast<volatile LPCG101_CONFIG*>(0x40CC6CB8); }
};

// LPCG access control
union LPCG101_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG101_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG101_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG101_AUTHEN*>(0x40CC6CBC); }
};

// LPCG direct control
union LPCG102_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG102_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG102_DIRECT &ref() { return *reinterpret_cast<volatile LPCG102_DIRECT*>(0x40CC6CC0); }
};

// LPCG domain control
union LPCG102_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG102_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG102_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG102_DOMAIN*>(0x40CC6CC4); }
};

// LPCG working status
union LPCG102_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG102_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG102_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG102_STATUS0*>(0x40CC6CD0); }
};

// LPCG low power status
union LPCG102_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG102_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG102_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG102_STATUS1*>(0x40CC6CD4); }
};

// LPCG configuration
union LPCG102_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG102_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG102_CONFIG &ref() { return *reinterpret_cast<volatile LPCG102_CONFIG*>(0x40CC6CD8); }
};

// LPCG access control
union LPCG102_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG102_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG102_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG102_AUTHEN*>(0x40CC6CDC); }
};

// LPCG direct control
union LPCG103_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG103_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG103_DIRECT &ref() { return *reinterpret_cast<volatile LPCG103_DIRECT*>(0x40CC6CE0); }
};

// LPCG domain control
union LPCG103_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG103_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG103_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG103_DOMAIN*>(0x40CC6CE4); }
};

// LPCG working status
union LPCG103_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG103_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG103_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG103_STATUS0*>(0x40CC6CF0); }
};

// LPCG low power status
union LPCG103_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG103_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG103_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG103_STATUS1*>(0x40CC6CF4); }
};

// LPCG configuration
union LPCG103_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG103_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG103_CONFIG &ref() { return *reinterpret_cast<volatile LPCG103_CONFIG*>(0x40CC6CF8); }
};

// LPCG access control
union LPCG103_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG103_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG103_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG103_AUTHEN*>(0x40CC6CFC); }
};

// LPCG direct control
union LPCG104_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG104_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG104_DIRECT &ref() { return *reinterpret_cast<volatile LPCG104_DIRECT*>(0x40CC6D00); }
};

// LPCG domain control
union LPCG104_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG104_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG104_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG104_DOMAIN*>(0x40CC6D04); }
};

// LPCG working status
union LPCG104_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG104_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG104_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG104_STATUS0*>(0x40CC6D10); }
};

// LPCG low power status
union LPCG104_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG104_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG104_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG104_STATUS1*>(0x40CC6D14); }
};

// LPCG configuration
union LPCG104_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG104_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG104_CONFIG &ref() { return *reinterpret_cast<volatile LPCG104_CONFIG*>(0x40CC6D18); }
};

// LPCG access control
union LPCG104_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG104_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG104_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG104_AUTHEN*>(0x40CC6D1C); }
};

// LPCG direct control
union LPCG105_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG105_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG105_DIRECT &ref() { return *reinterpret_cast<volatile LPCG105_DIRECT*>(0x40CC6D20); }
};

// LPCG domain control
union LPCG105_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG105_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG105_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG105_DOMAIN*>(0x40CC6D24); }
};

// LPCG working status
union LPCG105_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG105_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG105_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG105_STATUS0*>(0x40CC6D30); }
};

// LPCG low power status
union LPCG105_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG105_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG105_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG105_STATUS1*>(0x40CC6D34); }
};

// LPCG configuration
union LPCG105_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG105_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG105_CONFIG &ref() { return *reinterpret_cast<volatile LPCG105_CONFIG*>(0x40CC6D38); }
};

// LPCG access control
union LPCG105_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG105_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG105_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG105_AUTHEN*>(0x40CC6D3C); }
};

// LPCG direct control
union LPCG106_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG106_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG106_DIRECT &ref() { return *reinterpret_cast<volatile LPCG106_DIRECT*>(0x40CC6D40); }
};

// LPCG domain control
union LPCG106_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG106_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG106_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG106_DOMAIN*>(0x40CC6D44); }
};

// LPCG working status
union LPCG106_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG106_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG106_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG106_STATUS0*>(0x40CC6D50); }
};

// LPCG low power status
union LPCG106_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG106_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG106_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG106_STATUS1*>(0x40CC6D54); }
};

// LPCG configuration
union LPCG106_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG106_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG106_CONFIG &ref() { return *reinterpret_cast<volatile LPCG106_CONFIG*>(0x40CC6D58); }
};

// LPCG access control
union LPCG106_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG106_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG106_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG106_AUTHEN*>(0x40CC6D5C); }
};

// LPCG direct control
union LPCG107_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG107_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG107_DIRECT &ref() { return *reinterpret_cast<volatile LPCG107_DIRECT*>(0x40CC6D60); }
};

// LPCG domain control
union LPCG107_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG107_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG107_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG107_DOMAIN*>(0x40CC6D64); }
};

// LPCG working status
union LPCG107_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG107_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG107_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG107_STATUS0*>(0x40CC6D70); }
};

// LPCG low power status
union LPCG107_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG107_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG107_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG107_STATUS1*>(0x40CC6D74); }
};

// LPCG configuration
union LPCG107_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG107_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG107_CONFIG &ref() { return *reinterpret_cast<volatile LPCG107_CONFIG*>(0x40CC6D78); }
};

// LPCG access control
union LPCG107_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG107_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG107_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG107_AUTHEN*>(0x40CC6D7C); }
};

// LPCG direct control
union LPCG108_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG108_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG108_DIRECT &ref() { return *reinterpret_cast<volatile LPCG108_DIRECT*>(0x40CC6D80); }
};

// LPCG domain control
union LPCG108_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG108_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG108_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG108_DOMAIN*>(0x40CC6D84); }
};

// LPCG working status
union LPCG108_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG108_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG108_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG108_STATUS0*>(0x40CC6D90); }
};

// LPCG low power status
union LPCG108_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG108_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG108_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG108_STATUS1*>(0x40CC6D94); }
};

// LPCG configuration
union LPCG108_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG108_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG108_CONFIG &ref() { return *reinterpret_cast<volatile LPCG108_CONFIG*>(0x40CC6D98); }
};

// LPCG access control
union LPCG108_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG108_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG108_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG108_AUTHEN*>(0x40CC6D9C); }
};

// LPCG direct control
union LPCG109_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG109_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG109_DIRECT &ref() { return *reinterpret_cast<volatile LPCG109_DIRECT*>(0x40CC6DA0); }
};

// LPCG domain control
union LPCG109_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG109_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG109_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG109_DOMAIN*>(0x40CC6DA4); }
};

// LPCG working status
union LPCG109_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG109_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG109_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG109_STATUS0*>(0x40CC6DB0); }
};

// LPCG low power status
union LPCG109_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG109_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG109_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG109_STATUS1*>(0x40CC6DB4); }
};

// LPCG configuration
union LPCG109_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG109_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG109_CONFIG &ref() { return *reinterpret_cast<volatile LPCG109_CONFIG*>(0x40CC6DB8); }
};

// LPCG access control
union LPCG109_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG109_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG109_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG109_AUTHEN*>(0x40CC6DBC); }
};

// LPCG direct control
union LPCG110_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG110_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG110_DIRECT &ref() { return *reinterpret_cast<volatile LPCG110_DIRECT*>(0x40CC6DC0); }
};

// LPCG domain control
union LPCG110_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG110_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG110_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG110_DOMAIN*>(0x40CC6DC4); }
};

// LPCG working status
union LPCG110_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG110_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG110_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG110_STATUS0*>(0x40CC6DD0); }
};

// LPCG low power status
union LPCG110_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG110_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG110_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG110_STATUS1*>(0x40CC6DD4); }
};

// LPCG configuration
union LPCG110_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG110_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG110_CONFIG &ref() { return *reinterpret_cast<volatile LPCG110_CONFIG*>(0x40CC6DD8); }
};

// LPCG access control
union LPCG110_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG110_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG110_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG110_AUTHEN*>(0x40CC6DDC); }
};

// LPCG direct control
union LPCG111_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG111_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG111_DIRECT &ref() { return *reinterpret_cast<volatile LPCG111_DIRECT*>(0x40CC6DE0); }
};

// LPCG domain control
union LPCG111_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG111_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG111_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG111_DOMAIN*>(0x40CC6DE4); }
};

// LPCG working status
union LPCG111_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG111_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG111_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG111_STATUS0*>(0x40CC6DF0); }
};

// LPCG low power status
union LPCG111_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG111_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG111_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG111_STATUS1*>(0x40CC6DF4); }
};

// LPCG configuration
union LPCG111_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG111_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG111_CONFIG &ref() { return *reinterpret_cast<volatile LPCG111_CONFIG*>(0x40CC6DF8); }
};

// LPCG access control
union LPCG111_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG111_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG111_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG111_AUTHEN*>(0x40CC6DFC); }
};

// LPCG direct control
union LPCG112_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG112_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG112_DIRECT &ref() { return *reinterpret_cast<volatile LPCG112_DIRECT*>(0x40CC6E00); }
};

// LPCG domain control
union LPCG112_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG112_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG112_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG112_DOMAIN*>(0x40CC6E04); }
};

// LPCG working status
union LPCG112_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG112_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG112_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG112_STATUS0*>(0x40CC6E10); }
};

// LPCG low power status
union LPCG112_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG112_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG112_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG112_STATUS1*>(0x40CC6E14); }
};

// LPCG configuration
union LPCG112_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG112_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG112_CONFIG &ref() { return *reinterpret_cast<volatile LPCG112_CONFIG*>(0x40CC6E18); }
};

// LPCG access control
union LPCG112_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG112_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG112_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG112_AUTHEN*>(0x40CC6E1C); }
};

// LPCG direct control
union LPCG113_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG113_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG113_DIRECT &ref() { return *reinterpret_cast<volatile LPCG113_DIRECT*>(0x40CC6E20); }
};

// LPCG domain control
union LPCG113_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG113_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG113_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG113_DOMAIN*>(0x40CC6E24); }
};

// LPCG working status
union LPCG113_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG113_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG113_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG113_STATUS0*>(0x40CC6E30); }
};

// LPCG low power status
union LPCG113_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG113_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG113_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG113_STATUS1*>(0x40CC6E34); }
};

// LPCG configuration
union LPCG113_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG113_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG113_CONFIG &ref() { return *reinterpret_cast<volatile LPCG113_CONFIG*>(0x40CC6E38); }
};

// LPCG access control
union LPCG113_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG113_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG113_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG113_AUTHEN*>(0x40CC6E3C); }
};

// LPCG direct control
union LPCG114_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG114_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG114_DIRECT &ref() { return *reinterpret_cast<volatile LPCG114_DIRECT*>(0x40CC6E40); }
};

// LPCG domain control
union LPCG114_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG114_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG114_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG114_DOMAIN*>(0x40CC6E44); }
};

// LPCG working status
union LPCG114_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG114_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG114_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG114_STATUS0*>(0x40CC6E50); }
};

// LPCG low power status
union LPCG114_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG114_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG114_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG114_STATUS1*>(0x40CC6E54); }
};

// LPCG configuration
union LPCG114_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG114_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG114_CONFIG &ref() { return *reinterpret_cast<volatile LPCG114_CONFIG*>(0x40CC6E58); }
};

// LPCG access control
union LPCG114_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG114_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG114_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG114_AUTHEN*>(0x40CC6E5C); }
};

// LPCG direct control
union LPCG115_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG115_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG115_DIRECT &ref() { return *reinterpret_cast<volatile LPCG115_DIRECT*>(0x40CC6E60); }
};

// LPCG domain control
union LPCG115_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG115_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG115_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG115_DOMAIN*>(0x40CC6E64); }
};

// LPCG working status
union LPCG115_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG115_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG115_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG115_STATUS0*>(0x40CC6E70); }
};

// LPCG low power status
union LPCG115_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG115_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG115_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG115_STATUS1*>(0x40CC6E74); }
};

// LPCG configuration
union LPCG115_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG115_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG115_CONFIG &ref() { return *reinterpret_cast<volatile LPCG115_CONFIG*>(0x40CC6E78); }
};

// LPCG access control
union LPCG115_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG115_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG115_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG115_AUTHEN*>(0x40CC6E7C); }
};

// LPCG direct control
union LPCG116_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG116_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG116_DIRECT &ref() { return *reinterpret_cast<volatile LPCG116_DIRECT*>(0x40CC6E80); }
};

// LPCG domain control
union LPCG116_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG116_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG116_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG116_DOMAIN*>(0x40CC6E84); }
};

// LPCG working status
union LPCG116_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG116_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG116_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG116_STATUS0*>(0x40CC6E90); }
};

// LPCG low power status
union LPCG116_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG116_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG116_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG116_STATUS1*>(0x40CC6E94); }
};

// LPCG configuration
union LPCG116_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG116_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG116_CONFIG &ref() { return *reinterpret_cast<volatile LPCG116_CONFIG*>(0x40CC6E98); }
};

// LPCG access control
union LPCG116_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG116_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG116_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG116_AUTHEN*>(0x40CC6E9C); }
};

// LPCG direct control
union LPCG117_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG117_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG117_DIRECT &ref() { return *reinterpret_cast<volatile LPCG117_DIRECT*>(0x40CC6EA0); }
};

// LPCG domain control
union LPCG117_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG117_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG117_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG117_DOMAIN*>(0x40CC6EA4); }
};

// LPCG working status
union LPCG117_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG117_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG117_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG117_STATUS0*>(0x40CC6EB0); }
};

// LPCG low power status
union LPCG117_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG117_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG117_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG117_STATUS1*>(0x40CC6EB4); }
};

// LPCG configuration
union LPCG117_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG117_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG117_CONFIG &ref() { return *reinterpret_cast<volatile LPCG117_CONFIG*>(0x40CC6EB8); }
};

// LPCG access control
union LPCG117_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG117_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG117_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG117_AUTHEN*>(0x40CC6EBC); }
};

// LPCG direct control
union LPCG118_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG118_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG118_DIRECT &ref() { return *reinterpret_cast<volatile LPCG118_DIRECT*>(0x40CC6EC0); }
};

// LPCG domain control
union LPCG118_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG118_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG118_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG118_DOMAIN*>(0x40CC6EC4); }
};

// LPCG working status
union LPCG118_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG118_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG118_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG118_STATUS0*>(0x40CC6ED0); }
};

// LPCG low power status
union LPCG118_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG118_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG118_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG118_STATUS1*>(0x40CC6ED4); }
};

// LPCG configuration
union LPCG118_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG118_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG118_CONFIG &ref() { return *reinterpret_cast<volatile LPCG118_CONFIG*>(0x40CC6ED8); }
};

// LPCG access control
union LPCG118_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG118_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG118_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG118_AUTHEN*>(0x40CC6EDC); }
};

// LPCG direct control
union LPCG119_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG119_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG119_DIRECT &ref() { return *reinterpret_cast<volatile LPCG119_DIRECT*>(0x40CC6EE0); }
};

// LPCG domain control
union LPCG119_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG119_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG119_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG119_DOMAIN*>(0x40CC6EE4); }
};

// LPCG working status
union LPCG119_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG119_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG119_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG119_STATUS0*>(0x40CC6EF0); }
};

// LPCG low power status
union LPCG119_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG119_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG119_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG119_STATUS1*>(0x40CC6EF4); }
};

// LPCG configuration
union LPCG119_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG119_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG119_CONFIG &ref() { return *reinterpret_cast<volatile LPCG119_CONFIG*>(0x40CC6EF8); }
};

// LPCG access control
union LPCG119_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG119_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG119_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG119_AUTHEN*>(0x40CC6EFC); }
};

// LPCG direct control
union LPCG120_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG120_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG120_DIRECT &ref() { return *reinterpret_cast<volatile LPCG120_DIRECT*>(0x40CC6F00); }
};

// LPCG domain control
union LPCG120_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG120_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG120_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG120_DOMAIN*>(0x40CC6F04); }
};

// LPCG working status
union LPCG120_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG120_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG120_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG120_STATUS0*>(0x40CC6F10); }
};

// LPCG low power status
union LPCG120_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG120_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG120_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG120_STATUS1*>(0x40CC6F14); }
};

// LPCG configuration
union LPCG120_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG120_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG120_CONFIG &ref() { return *reinterpret_cast<volatile LPCG120_CONFIG*>(0x40CC6F18); }
};

// LPCG access control
union LPCG120_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG120_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG120_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG120_AUTHEN*>(0x40CC6F1C); }
};

// LPCG direct control
union LPCG121_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG121_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG121_DIRECT &ref() { return *reinterpret_cast<volatile LPCG121_DIRECT*>(0x40CC6F20); }
};

// LPCG domain control
union LPCG121_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG121_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG121_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG121_DOMAIN*>(0x40CC6F24); }
};

// LPCG working status
union LPCG121_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG121_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG121_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG121_STATUS0*>(0x40CC6F30); }
};

// LPCG low power status
union LPCG121_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG121_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG121_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG121_STATUS1*>(0x40CC6F34); }
};

// LPCG configuration
union LPCG121_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG121_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG121_CONFIG &ref() { return *reinterpret_cast<volatile LPCG121_CONFIG*>(0x40CC6F38); }
};

// LPCG access control
union LPCG121_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG121_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG121_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG121_AUTHEN*>(0x40CC6F3C); }
};

// LPCG direct control
union LPCG122_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG122_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG122_DIRECT &ref() { return *reinterpret_cast<volatile LPCG122_DIRECT*>(0x40CC6F40); }
};

// LPCG domain control
union LPCG122_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG122_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG122_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG122_DOMAIN*>(0x40CC6F44); }
};

// LPCG working status
union LPCG122_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG122_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG122_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG122_STATUS0*>(0x40CC6F50); }
};

// LPCG low power status
union LPCG122_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG122_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG122_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG122_STATUS1*>(0x40CC6F54); }
};

// LPCG configuration
union LPCG122_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG122_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG122_CONFIG &ref() { return *reinterpret_cast<volatile LPCG122_CONFIG*>(0x40CC6F58); }
};

// LPCG access control
union LPCG122_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG122_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG122_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG122_AUTHEN*>(0x40CC6F5C); }
};

// LPCG direct control
union LPCG123_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG123_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG123_DIRECT &ref() { return *reinterpret_cast<volatile LPCG123_DIRECT*>(0x40CC6F60); }
};

// LPCG domain control
union LPCG123_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG123_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG123_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG123_DOMAIN*>(0x40CC6F64); }
};

// LPCG working status
union LPCG123_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG123_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG123_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG123_STATUS0*>(0x40CC6F70); }
};

// LPCG low power status
union LPCG123_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG123_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG123_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG123_STATUS1*>(0x40CC6F74); }
};

// LPCG configuration
union LPCG123_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG123_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG123_CONFIG &ref() { return *reinterpret_cast<volatile LPCG123_CONFIG*>(0x40CC6F78); }
};

// LPCG access control
union LPCG123_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG123_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG123_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG123_AUTHEN*>(0x40CC6F7C); }
};

// LPCG direct control
union LPCG124_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG124_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG124_DIRECT &ref() { return *reinterpret_cast<volatile LPCG124_DIRECT*>(0x40CC6F80); }
};

// LPCG domain control
union LPCG124_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG124_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG124_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG124_DOMAIN*>(0x40CC6F84); }
};

// LPCG working status
union LPCG124_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG124_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG124_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG124_STATUS0*>(0x40CC6F90); }
};

// LPCG low power status
union LPCG124_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG124_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG124_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG124_STATUS1*>(0x40CC6F94); }
};

// LPCG configuration
union LPCG124_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG124_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG124_CONFIG &ref() { return *reinterpret_cast<volatile LPCG124_CONFIG*>(0x40CC6F98); }
};

// LPCG access control
union LPCG124_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG124_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG124_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG124_AUTHEN*>(0x40CC6F9C); }
};

// LPCG direct control
union LPCG125_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG125_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG125_DIRECT &ref() { return *reinterpret_cast<volatile LPCG125_DIRECT*>(0x40CC6FA0); }
};

// LPCG domain control
union LPCG125_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG125_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG125_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG125_DOMAIN*>(0x40CC6FA4); }
};

// LPCG working status
union LPCG125_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG125_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG125_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG125_STATUS0*>(0x40CC6FB0); }
};

// LPCG low power status
union LPCG125_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG125_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG125_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG125_STATUS1*>(0x40CC6FB4); }
};

// LPCG configuration
union LPCG125_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG125_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG125_CONFIG &ref() { return *reinterpret_cast<volatile LPCG125_CONFIG*>(0x40CC6FB8); }
};

// LPCG access control
union LPCG125_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG125_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG125_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG125_AUTHEN*>(0x40CC6FBC); }
};

// LPCG direct control
union LPCG126_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG126_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG126_DIRECT &ref() { return *reinterpret_cast<volatile LPCG126_DIRECT*>(0x40CC6FC0); }
};

// LPCG domain control
union LPCG126_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG126_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG126_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG126_DOMAIN*>(0x40CC6FC4); }
};

// LPCG working status
union LPCG126_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG126_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG126_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG126_STATUS0*>(0x40CC6FD0); }
};

// LPCG low power status
union LPCG126_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG126_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG126_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG126_STATUS1*>(0x40CC6FD4); }
};

// LPCG configuration
union LPCG126_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG126_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG126_CONFIG &ref() { return *reinterpret_cast<volatile LPCG126_CONFIG*>(0x40CC6FD8); }
};

// LPCG access control
union LPCG126_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG126_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG126_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG126_AUTHEN*>(0x40CC6FDC); }
};

// LPCG direct control
union LPCG127_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG127_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG127_DIRECT &ref() { return *reinterpret_cast<volatile LPCG127_DIRECT*>(0x40CC6FE0); }
};

// LPCG domain control
union LPCG127_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG127_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG127_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG127_DOMAIN*>(0x40CC6FE4); }
};

// LPCG working status
union LPCG127_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG127_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG127_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG127_STATUS0*>(0x40CC6FF0); }
};

// LPCG low power status
union LPCG127_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG127_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG127_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG127_STATUS1*>(0x40CC6FF4); }
};

// LPCG configuration
union LPCG127_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG127_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG127_CONFIG &ref() { return *reinterpret_cast<volatile LPCG127_CONFIG*>(0x40CC6FF8); }
};

// LPCG access control
union LPCG127_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG127_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG127_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG127_AUTHEN*>(0x40CC6FFC); }
};

// LPCG direct control
union LPCG128_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG128_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG128_DIRECT &ref() { return *reinterpret_cast<volatile LPCG128_DIRECT*>(0x40CC7000); }
};

// LPCG domain control
union LPCG128_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG128_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG128_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG128_DOMAIN*>(0x40CC7004); }
};

// LPCG working status
union LPCG128_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG128_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG128_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG128_STATUS0*>(0x40CC7010); }
};

// LPCG low power status
union LPCG128_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG128_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG128_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG128_STATUS1*>(0x40CC7014); }
};

// LPCG configuration
union LPCG128_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG128_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG128_CONFIG &ref() { return *reinterpret_cast<volatile LPCG128_CONFIG*>(0x40CC7018); }
};

// LPCG access control
union LPCG128_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG128_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG128_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG128_AUTHEN*>(0x40CC701C); }
};

// LPCG direct control
union LPCG129_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG129_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG129_DIRECT &ref() { return *reinterpret_cast<volatile LPCG129_DIRECT*>(0x40CC7020); }
};

// LPCG domain control
union LPCG129_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG129_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG129_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG129_DOMAIN*>(0x40CC7024); }
};

// LPCG working status
union LPCG129_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG129_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG129_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG129_STATUS0*>(0x40CC7030); }
};

// LPCG low power status
union LPCG129_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG129_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG129_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG129_STATUS1*>(0x40CC7034); }
};

// LPCG configuration
union LPCG129_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG129_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG129_CONFIG &ref() { return *reinterpret_cast<volatile LPCG129_CONFIG*>(0x40CC7038); }
};

// LPCG access control
union LPCG129_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG129_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG129_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG129_AUTHEN*>(0x40CC703C); }
};

// LPCG direct control
union LPCG130_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG130_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG130_DIRECT &ref() { return *reinterpret_cast<volatile LPCG130_DIRECT*>(0x40CC7040); }
};

// LPCG domain control
union LPCG130_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG130_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG130_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG130_DOMAIN*>(0x40CC7044); }
};

// LPCG working status
union LPCG130_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG130_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG130_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG130_STATUS0*>(0x40CC7050); }
};

// LPCG low power status
union LPCG130_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG130_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG130_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG130_STATUS1*>(0x40CC7054); }
};

// LPCG configuration
union LPCG130_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG130_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG130_CONFIG &ref() { return *reinterpret_cast<volatile LPCG130_CONFIG*>(0x40CC7058); }
};

// LPCG access control
union LPCG130_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG130_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG130_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG130_AUTHEN*>(0x40CC705C); }
};

// LPCG direct control
union LPCG131_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG131_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG131_DIRECT &ref() { return *reinterpret_cast<volatile LPCG131_DIRECT*>(0x40CC7060); }
};

// LPCG domain control
union LPCG131_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG131_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG131_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG131_DOMAIN*>(0x40CC7064); }
};

// LPCG working status
union LPCG131_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG131_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG131_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG131_STATUS0*>(0x40CC7070); }
};

// LPCG low power status
union LPCG131_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG131_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG131_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG131_STATUS1*>(0x40CC7074); }
};

// LPCG configuration
union LPCG131_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG131_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG131_CONFIG &ref() { return *reinterpret_cast<volatile LPCG131_CONFIG*>(0x40CC7078); }
};

// LPCG access control
union LPCG131_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG131_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG131_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG131_AUTHEN*>(0x40CC707C); }
};

// LPCG direct control
union LPCG132_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG132_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG132_DIRECT &ref() { return *reinterpret_cast<volatile LPCG132_DIRECT*>(0x40CC7080); }
};

// LPCG domain control
union LPCG132_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG132_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG132_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG132_DOMAIN*>(0x40CC7084); }
};

// LPCG working status
union LPCG132_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG132_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG132_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG132_STATUS0*>(0x40CC7090); }
};

// LPCG low power status
union LPCG132_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG132_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG132_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG132_STATUS1*>(0x40CC7094); }
};

// LPCG configuration
union LPCG132_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG132_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG132_CONFIG &ref() { return *reinterpret_cast<volatile LPCG132_CONFIG*>(0x40CC7098); }
};

// LPCG access control
union LPCG132_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG132_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG132_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG132_AUTHEN*>(0x40CC709C); }
};

// LPCG direct control
union LPCG133_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG133_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG133_DIRECT &ref() { return *reinterpret_cast<volatile LPCG133_DIRECT*>(0x40CC70A0); }
};

// LPCG domain control
union LPCG133_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG133_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG133_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG133_DOMAIN*>(0x40CC70A4); }
};

// LPCG working status
union LPCG133_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG133_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG133_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG133_STATUS0*>(0x40CC70B0); }
};

// LPCG low power status
union LPCG133_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG133_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG133_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG133_STATUS1*>(0x40CC70B4); }
};

// LPCG configuration
union LPCG133_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG133_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG133_CONFIG &ref() { return *reinterpret_cast<volatile LPCG133_CONFIG*>(0x40CC70B8); }
};

// LPCG access control
union LPCG133_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG133_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG133_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG133_AUTHEN*>(0x40CC70BC); }
};

// LPCG direct control
union LPCG134_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG134_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG134_DIRECT &ref() { return *reinterpret_cast<volatile LPCG134_DIRECT*>(0x40CC70C0); }
};

// LPCG domain control
union LPCG134_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG134_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG134_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG134_DOMAIN*>(0x40CC70C4); }
};

// LPCG working status
union LPCG134_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG134_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG134_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG134_STATUS0*>(0x40CC70D0); }
};

// LPCG low power status
union LPCG134_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG134_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG134_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG134_STATUS1*>(0x40CC70D4); }
};

// LPCG configuration
union LPCG134_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG134_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG134_CONFIG &ref() { return *reinterpret_cast<volatile LPCG134_CONFIG*>(0x40CC70D8); }
};

// LPCG access control
union LPCG134_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG134_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG134_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG134_AUTHEN*>(0x40CC70DC); }
};

// LPCG direct control
union LPCG135_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG135_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG135_DIRECT &ref() { return *reinterpret_cast<volatile LPCG135_DIRECT*>(0x40CC70E0); }
};

// LPCG domain control
union LPCG135_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG135_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG135_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG135_DOMAIN*>(0x40CC70E4); }
};

// LPCG working status
union LPCG135_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG135_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG135_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG135_STATUS0*>(0x40CC70F0); }
};

// LPCG low power status
union LPCG135_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG135_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG135_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG135_STATUS1*>(0x40CC70F4); }
};

// LPCG configuration
union LPCG135_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG135_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG135_CONFIG &ref() { return *reinterpret_cast<volatile LPCG135_CONFIG*>(0x40CC70F8); }
};

// LPCG access control
union LPCG135_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG135_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG135_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG135_AUTHEN*>(0x40CC70FC); }
};

// LPCG direct control
union LPCG136_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG136_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG136_DIRECT &ref() { return *reinterpret_cast<volatile LPCG136_DIRECT*>(0x40CC7100); }
};

// LPCG domain control
union LPCG136_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG136_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG136_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG136_DOMAIN*>(0x40CC7104); }
};

// LPCG working status
union LPCG136_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG136_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG136_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG136_STATUS0*>(0x40CC7110); }
};

// LPCG low power status
union LPCG136_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG136_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG136_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG136_STATUS1*>(0x40CC7114); }
};

// LPCG configuration
union LPCG136_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG136_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG136_CONFIG &ref() { return *reinterpret_cast<volatile LPCG136_CONFIG*>(0x40CC7118); }
};

// LPCG access control
union LPCG136_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG136_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG136_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG136_AUTHEN*>(0x40CC711C); }
};

// LPCG direct control
union LPCG137_DIRECT {
  
  // LPCG on
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - LPCG on
    eON ON : 1;
    uint32_t _reserved_0 : 31;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG137_DIRECT() = delete;
  inline void Reset() volatile { this->value = 0x00000001; }
  static inline volatile LPCG137_DIRECT &ref() { return *reinterpret_cast<volatile LPCG137_DIRECT*>(0x40CC7120); }
};

// LPCG domain control
union LPCG137_DOMAIN {
  
  // Current dependence level
  enum class eLEVEL : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL0 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL1 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL2 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Depend level
  enum class eLEVEL3 : uint32_t {
    // This clock source is not needed in any mode, and can be turned off
    eNO = 0,
    // This clock source is needed in RUN mode, but not needed in WAIT, STOP mode
    eR = 1,
    // This clock source is needed in RUN and WAIT mode, but not needed in STOP mode
    eRW = 2,
    // This clock source is needed in RUN, WAIT and STOP mode
    eRWS = 3,
    // This clock source is always on in any mode (including SUSPEND)
    eALL = 4,
  };
  
  // Bit field definition.
  struct {
    // read-write - Current dependence level
    eLEVEL LEVEL : 3;
    uint32_t _reserved_0 : 13;
    // read-write - Depend level
    eLEVEL0 LEVEL0 : 3;
    uint32_t _reserved_1 : 1;
    // read-write - Depend level
    eLEVEL1 LEVEL1 : 3;
    uint32_t _reserved_2 : 1;
    // read-write - Depend level
    eLEVEL2 LEVEL2 : 3;
    uint32_t _reserved_3 : 1;
    // read-write - Depend level
    eLEVEL3 LEVEL3 : 3;
    uint32_t _reserved_4 : 1;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG137_DOMAIN() = delete;
  inline void Reset() volatile { this->value = 0x00010001; }
  static inline volatile LPCG137_DOMAIN &ref() { return *reinterpret_cast<volatile LPCG137_DOMAIN*>(0x40CC7124); }
};

// LPCG working status
union LPCG137_STATUS0 {
  
  // LPCG current state
  enum class eON : uint32_t {
    // LPCG is OFF.
    eON_0 = 0,
    // LPCG is ON.
    eON_1 = 1,
  };
  
  // Domains that own this clock gate
  enum class eACTIVE_DOMAIN : uint32_t {
    // Clock not owned by any domain
    eNO = 0,
    // Clock owned by Domain0
    eZERO = 1,
    // Clock owned by Domain1
    eTWO = 2,
    // Clock owned by Domain0 and Domain1
    eTHREE = 3,
    // Clock owned by Domain2
    eFOUR = 4,
    // Clock owned by Domain0 and Domain2
    eFIVE = 5,
    // Clock owned by Domain1 and Domain2
    eSIX = 6,
    // Clock owned by Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Clock owned by Domain3
    eEIGHT = 8,
    // Clock owned by Domain0 and Domain3
    eNINE = 9,
    // Clock owned by Domain1 and Domain3
    eTEN = 10,
    // Clock owned by Domain2 and Domain3
    eELV = 11,
    // Clock owned by Domain0, Domain 1, and Domain3
    eTW = 12,
    // Clock owned by Domain0, Domain 2, and Domain3
    eTH = 13,
    // Clock owned by Domain1, Domain 2, and Domain3
    eFR = 14,
    // Clock owned by all domains
    eall = 15,
  };
  
  // Enable status from each domain
  enum class eDOMAIN_ENABLE : uint32_t {
    // No domain request
    eNO = 0,
    // Request from Domain0
    eZERO = 1,
    // Request from Domain1
    eTWO = 2,
    // Request from Domain0 and Domain1
    eTHREE = 3,
    // Request from Domain2
    eFOUR = 4,
    // Request from Domain0 and Domain2
    eFIVE = 5,
    // Request from Domain1 and Domain2
    eSIX = 6,
    // Request from Domain0, Domain1 and Domain 2
    eSEVEN = 7,
    // Request from Domain3
    eEIGHT = 8,
    // Request from Domain0 and Domain3
    eNINE = 9,
    // Request from Domain1 and Domain3
    eTEN = 10,
    // Request from Domain2 and Domain3
    eELV = 11,
    // Request from Domain0, Domain 1, and Domain3
    eTW = 12,
    // Request from Domain0, Domain 2, and Domain3
    eTH = 13,
    // Request from Domain1, Domain 2, and Domain3
    eFR = 14,
    // Request from all domains
    eall = 15,
  };
  
  // Bit field definition.
  struct {
    // read-only - LPCG current state
    eON ON : 1;
    uint32_t _reserved_0 : 7;
    // read-only - Domains that own this clock gate
    eACTIVE_DOMAIN ACTIVE_DOMAIN : 4;
    // read-only - Enable status from each domain
    eDOMAIN_ENABLE DOMAIN_ENABLE : 4;
    uint32_t _reserved_1 : 16;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG137_STATUS0() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG137_STATUS0 &ref() { return *reinterpret_cast<volatile LPCG137_STATUS0*>(0x40CC7130); }
};

// LPCG low power status
union LPCG137_STATUS1 {
  
  // Domain0 Low Power Mode
  enum class eCPU0_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain0 request enter Low Power Mode
  enum class eCPU0_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain0 Low Power Mode task done
  enum class eCPU0_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode
  enum class eCPU1_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain1 request enter Low Power Mode
  enum class eCPU1_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain1 Low Power Mode task done
  enum class eCPU1_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode
  enum class eCPU2_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain2 request enter Low Power Mode
  enum class eCPU2_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain2 Low Power Mode task done
  enum class eCPU2_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode
  enum class eCPU3_MODE : uint32_t {
    // Run
    eRUN = 0,
    // Wait
    eWAIT = 1,
    // Stop
    eSTOP = 2,
    // Suspend
    eSUSPEND = 3,
  };
  
  // Domain3 request enter Low Power Mode
  enum class eCPU3_MODE_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Request from domain to enter Low Power Mode
    eENABLE = 1,
  };
  
  // Domain3 Low Power Mode task done
  enum class eCPU3_MODE_DONE : uint32_t {
    // Clock is not gated
    eDIS = 0,
    // Clock is gated-off
    eENABLE = 1,
  };
  
  // Clock gate turn off request from GPC Setpoint
  enum class eSETPOINT_OFF_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned off
    eENABLE = 1,
  };
  
  // Clock gate turn off finish from GPC Setpoint
  enum class eSETPOINT_OFF_DONE : uint32_t {
    // Clock gate is not turned off
    eDIS = 0,
    // Clock gate is turned off
    eENABLE = 1,
  };
  
  // Clock gate turn on request from GPC Setpoint
  enum class eSETPOINT_ON_REQUEST : uint32_t {
    // No request
    eDIS = 0,
    // Clock gate requested to be turned on
    eENABLE = 1,
  };
  
  // Clock gate turn on finish from GPC Setpoint
  enum class eSETPOINT_ON_DONE : uint32_t {
    // Clock gate is not turned on
    eDIS = 0,
    // Clock gate is turned on
    eENABLE = 1,
  };
  
  // Bit field definition.
  struct {
    // read-only - Domain0 Low Power Mode
    eCPU0_MODE CPU0_MODE : 2;
    // read-only - Domain0 request enter Low Power Mode
    eCPU0_MODE_REQUEST CPU0_MODE_REQUEST : 1;
    // read-only - Domain0 Low Power Mode task done
    eCPU0_MODE_DONE CPU0_MODE_DONE : 1;
    // read-only - Domain1 Low Power Mode
    eCPU1_MODE CPU1_MODE : 2;
    // read-only - Domain1 request enter Low Power Mode
    eCPU1_MODE_REQUEST CPU1_MODE_REQUEST : 1;
    // read-only - Domain1 Low Power Mode task done
    eCPU1_MODE_DONE CPU1_MODE_DONE : 1;
    // read-only - Domain2 Low Power Mode
    eCPU2_MODE CPU2_MODE : 2;
    // read-only - Domain2 request enter Low Power Mode
    eCPU2_MODE_REQUEST CPU2_MODE_REQUEST : 1;
    // read-only - Domain2 Low Power Mode task done
    eCPU2_MODE_DONE CPU2_MODE_DONE : 1;
    // read-only - Domain3 Low Power Mode
    eCPU3_MODE CPU3_MODE : 2;
    // read-only - Domain3 request enter Low Power Mode
    eCPU3_MODE_REQUEST CPU3_MODE_REQUEST : 1;
    // read-only - Domain3 Low Power Mode task done
    eCPU3_MODE_DONE CPU3_MODE_DONE : 1;
    // read-only - Next Setpoint to change to
    uint32_t TARGET_SETPOINT : 4;
    // read-only - Current Setpoint
    uint32_t CURRENT_SETPOINT : 4;
    // read-only - Clock gate turn off request from GPC Setpoint
    eSETPOINT_OFF_REQUEST SETPOINT_OFF_REQUEST : 1;
    // read-only - Clock gate turn off finish from GPC Setpoint
    eSETPOINT_OFF_DONE SETPOINT_OFF_DONE : 1;
    // read-only - Clock gate turn on request from GPC Setpoint
    eSETPOINT_ON_REQUEST SETPOINT_ON_REQUEST : 1;
    // read-only - Clock gate turn on finish from GPC Setpoint
    eSETPOINT_ON_DONE SETPOINT_ON_DONE : 1;
    uint32_t _reserved_0 : 4;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG137_STATUS1() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG137_STATUS1 &ref() { return *reinterpret_cast<volatile LPCG137_STATUS1*>(0x40CC7134); }
};

// LPCG configuration
union LPCG137_CONFIG {
  
  // Setpoint present
  enum class eSETPOINT_PRESENT : uint32_t {
    // Setpoint is not implemented.
    eSETPOINT_PRESENT_0 = 0,
    // Setpoint is implemented.
    eSETPOINT_PRESENT_1 = 1,
  };
  
  // Bit field definition.
  struct {
    uint32_t _reserved_0 : 4;
    // read-only - Setpoint present
    eSETPOINT_PRESENT SETPOINT_PRESENT : 1;
    uint32_t _reserved_1 : 27;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG137_CONFIG() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG137_CONFIG &ref() { return *reinterpret_cast<volatile LPCG137_CONFIG*>(0x40CC7138); }
};

// LPCG access control
union LPCG137_AUTHEN {
  
  // User access
  enum class eTZ_USER : uint32_t {
    // LPCG cannot be changed in user mode.
    eTZ_USER_0 = 0,
    // LPCG can be changed in user mode.
    eTZ_USER_1 = 1,
  };
  
  // Non-secure access
  enum class eTZ_NS : uint32_t {
    // Cannot be changed in Non-secure mode.
    eTZ_NS_0 = 0,
    // Can be changed in Non-secure mode.
    eTZ_NS_1 = 1,
  };
  
  // lock truszone setting
  enum class eLOCK_TZ : uint32_t {
    // Trustzone setting is not locked.
    eLOCK_TZ_0 = 0,
    // Trustzone setting is locked.
    eLOCK_TZ_1 = 1,
  };
  
  // Lock Whitelist
  enum class eLOCK_LIST : uint32_t {
    // Whitelist is not locked.
    eLOCK_LIST_0 = 0,
    // Whitelist is locked.
    eLOCK_LIST_1 = 1,
  };
  
  // Low power and access control by domain
  enum class eDOMAIN_MODE : uint32_t {
    // Clock does not work in Domain Mode
    eDOMAIN_MODE_0 = 0,
    // Clock works in Domain Mode
    eDOMAIN_MODE_1 = 1,
  };
  
  // Low power and access control by Setpoint
  enum class eSETPOINT_MODE : uint32_t {
    // LPCG is not functioning in Setpoint controlled Mode
    eDISABLE = 0,
    // LPCG is functioning in Setpoint controlled Mode
    eENABLE = 1,
  };
  
  // CPU Low Power Mode
  enum class eCPULPM : uint32_t {
    // LPCG is not functioning in Low power Mode
    eDISABLE = 0,
    // LPCG is functioning in Low Power Mode
    eENABLE = 1,
  };
  
  // Lock low power and access mode
  enum class eLOCK_MODE : uint32_t {
    // MODE is not locked.
    eLOCK_MODE_0 = 0,
    // MODE is locked.
    eLOCK_MODE_1 = 1,
  };
  
  // Bit field definition.
  struct {
    // read-write - User access
    eTZ_USER TZ_USER : 1;
    // read-write - Non-secure access
    eTZ_NS TZ_NS : 1;
    uint32_t _reserved_0 : 2;
    // read-write - lock truszone setting
    eLOCK_TZ LOCK_TZ : 1;
    uint32_t _reserved_1 : 3;
    // read-write - Whitelist
    uint32_t WHITE_LIST : 4;
    // read-write - Lock Whitelist
    eLOCK_LIST LOCK_LIST : 1;
    uint32_t _reserved_2 : 3;
    // read-write - Low power and access control by domain
    eDOMAIN_MODE DOMAIN_MODE : 1;
    // read-write - Low power and access control by Setpoint
    eSETPOINT_MODE SETPOINT_MODE : 1;
    // read-write - CPU Low Power Mode
    eCPULPM CPULPM : 1;
    uint32_t _reserved_3 : 1;
    // read-write - Lock low power and access mode
    eLOCK_MODE LOCK_MODE : 1;
    uint32_t _reserved_4 : 11;
  } bits;
  
  // Full 32-bit register value.
  uint32_t value;

  LPCG137_AUTHEN() = delete;
  inline void Reset() volatile { this->value = 0x00000000; }
  static inline volatile LPCG137_AUTHEN &ref() { return *reinterpret_cast<volatile LPCG137_AUTHEN*>(0x40CC713C); }
};





} // namespace nCCM