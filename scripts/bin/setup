#!/bin/bash
set -e

export ANSIBLE_CONFIG=$FORGE_ROOT/ansible.cfg

ansible-playbook -i $FORGE_ROOT/ansible/local.ini \
  $FORGE_ROOT/ansible/playbooks/install_poetry_environment.yml \
  -e "package_path=${PROJECT_ROOT}/scripts/package"
