#!/bin/bash

# Only executes LinkServer command if one is not already running
if pgrep -x "LinkServer" > /dev/null
then
    echo "Process is already running."
else
    exec /usr/local/LinkServer_1.4.85/LinkServer ${@}
fi
