load("@rules_cc//cc:defs.bzl", "cc_binary")

cc_binary(
    name = "smoke",
    srcs = ["smoke.cpp"],
    # No std lib at all — just verify the toolchain links a bare main().
    linkopts = [
        "-nostartfiles",
        "-Wl,--unresolved-symbols=ignore-all",
    ],
)
